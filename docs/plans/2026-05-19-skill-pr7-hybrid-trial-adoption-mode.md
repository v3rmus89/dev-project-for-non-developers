# Skill PR #7 — hybrid real-project trial + adoption-mode redesign

> Hybrid PR (per user decision 2026-05-19): runs the **real-project trial** on `~/Desktop/Code/Boxette/call-details/` AND ships the **adoption-mode UX redesign** (`--mode=adopt` analyze-then-decide-with-owner). The trial's `--dry-run` / `--diff` is the empirical data source for the adoption-mode recommendation heuristics; the trial ends with a real `--apply --mode=adopt` against `call-details/` using the new policies.

## Context

PR #1–#6 shipped: Python + Node-TS + Go (PR #1–#3), two-tier code review + plan-loop improvements (PR #4), observability + self-improvement layer (PR #5), uv support for Python (PR #6). The current adoption contract (PR #1, unchanged through PR #6) is **all-or-nothing**: `bootstrap.py --apply` aborts with "collision detected" if any target file already exists, unless `--overwrite-existing` is passed (which then overwrites every colliding file). For a real-world existing project like `call-details/` — which has 4 high-value files the skill writes (`CLAUDE.md`, `pyproject.toml`, `.python-version`, `.gitignore`) AND 15 cleanly-writable files — neither option is acceptable: aborting blocks the user from any benefit; overwriting destroys irreplaceable domain content (call-details' `CLAUDE.md` has ~96 lines (snapshot as of 2026-05-19) of PII rules, architecture map, business TZ, onlinepbx specifics).

**Empirical collision baseline** (closes Codex iter-1 #2 — earlier draft claimed 8 collisions including `README.md`, `uv.lock`, `src/`, `tests/`; verified false via actual `bootstrap.py --dry-run --language python --project-name call-details --out ~/Desktop/Code/Boxette/call-details/`):

- **4 MODIFY (collisions, existing files the skill would write)**: `.gitignore`, `.python-version`, `CLAUDE.md`, `pyproject.toml`
- **15 CREATE (clean writes, no target file exists)**: `.editorconfig`, `.github/pull_request_template.md`, `.github/workflows/ci.yml`, `.pre-commit-config.yaml`, `AGENTS.md`, `BACKLOG.md`, `CONTRIBUTING.md`, `LESSONS.md`, `Makefile`, `docs/plans/README.md`, `pytest.ini`, `ruff.toml`, `scripts/run-with-clean-env.py`, `src/main.py`, `tests/test_smoke.py`
- **Gitignore caveat for `AGENTS.md`** (closes Codex iter-6 #4 — verified empirically: `git check-ignore -v` in `call-details/` returns `.gitignore:48:AGENTS.md`): the target's `.gitignore` deliberately ignores `AGENTS.md` (comment in target's `.gitignore` line 46-47: "Repo-local Codex config belongs in `~/.codex/`, not here"). A vanilla CREATE write would land on disk but never appear in `git diff --stat` or be committed → misleading trial result. **Phase D preflight runs `git check-ignore -v --` against every planned create** (closes self-check iter-6.5 #4 — earlier wording attributed the check to Phase A but the actual execution happens in Phase D preflight, before rehearsal-on-minimal-copy; Phase A is the empirical-findings phase that documents what the gitignore preflight reported, not the phase that runs it). Any matched file → owner decision recorded in trial-report Section (b) (closes self-check iter-6.5 #5 — Section (a) is pure inventory per Scope #11; decision/rationale content belongs in (b) "Phase A empirical findings + rehearsal-decision-script"): (a) skip it (SKIP policy), (b) write `.new` for manual review, OR (c) explicitly modify target's `.gitignore` to un-ignore + record rationale in Section (b).

`README.md` and `uv.lock` exist in `call-details/` but the skill never writes them, so they're not collisions. `src/` and `tests/` directories exist but the skill writes `src/main.py` and `tests/test_smoke.py` — file-inside-existing-directory, not collision.

PR #7 ships `--mode=adopt` to replace the all-or-nothing dichotomy with a **per-file analyze-then-decide-with-owner** contract. Per user 2026-05-19, the contract is **content-driven** (not a hardcoded policy table) — different projects need different choices; the analyzer + recommendation engine is the value, not a static rule set.

### Sequencing within PR #7

The trial and the redesign are intertwined because each informs the other:

**Temporal ordering** (closes self-check iter-5.5 #1 + #5 — earlier wording presented A→B→C as a temporal list, but iter-5 #4 made A post-impl, which means B (which validates A) must also be post-impl; only C is pre-A/B): impl-PR opens first → Phase C ships the code → Phase A + Phase B run as **post-impl validation gates** against the live target → Phase D = real `--apply --mode=adopt` against `call-details/` → Phase E = trial-surfaced docs + BACKLOG.

1. **Phase A — Trial baseline (post-implementation gate).** AFTER Phase C ships the impl PR, run `bootstrap.py --dry-run --language python --project-name call-details --out ~/Desktop/Code/Boxette/call-details/` (PR #6 contract) to confirm the collision manifest. (Already run during plan iter-1 fold for the iter-1 #2 baseline; the canonical Phase A execution happens AFTER impl.) Run `bootstrap.py --diff ...` to inspect each of the 4 collisions. Capture findings as a per-file empirical dataset.
2. **Phase B — Adoption-mode validation (post-implementation, alongside Phase A; HARD GATE for Phase D live apply per Codex iter-7 #4).** Validate the locked Scope #5 heuristics against Phase A's per-file evidence: does each rule produce a recommendation a thoughtful human would agree with? Heuristics are LOCKED at iter-2 — Phase B is the **gate** for whether Phase D can proceed: if any `manual_review_needed=false` recommendation is rejected by the owner (i.e. the heuristic misfired on a "safe" auto-apply path), Phase D MUST NOT run live until the misfiring rule is fixed. **Fix location** (closes Codex Tier-2 PR #16 #8 — earlier wording required "Phase C amended in a follow-up PR" but Phase 2 gate 9 requires the live trial to complete IN THIS PR; misfire would deadlock the PR's own acceptance gates with no in-PR resolution path): the heuristic fix is folded into THIS implementation PR by looping back to Phase C (amend `bootstrap_lib/adopt.py` recommend_policy + tests + smoke fixtures, re-run gates 2-8, then re-run Phase B). Follow-up PR is reserved only for cases the owner explicitly defers as out-of-scope (e.g. a new rule that needs Phase A empirical data from a *second* target project before being added — call this out in trial-report Section (e)). Also closes Codex Tier-2 PR #16 #1 — the earlier wording offered an "owner override via the interactive prompt during rehearsal" alternative, but Scope #6 + Bucket C only prompt `manual_review_needed=true` files, so that override path was impossible to execute; removed. Documentation-only "park to follow-up PR" is acceptable for `manual_review_needed=true` recommendations the owner rejects (those go through the prompt anyway); NOT acceptable for `manual_review_needed=false` recommendations that misfire (those auto-apply silently and can corrupt user files) — those block Phase D until the rule is fixed (in-PR loop-back, per above).
3. **Phase C — Adoption-mode implementation (FIRST in time).** Ship `--mode=adopt` per the locked-at-iter-2 design. Comprehensive test coverage (unit + integration + smoke). Phase C precedes A + B in time even though the plan-section ordering presents them in their conceptual order (Trial first conceptually = "what does the trial look like"; impl first temporally = "what code has to exist for the trial").
4. **Phase D — Trial completion.** Phase D **preflight** (closes Codex iter-1 #4 — `call-details/` is currently a live working repo; the restore manifest only protects bootstrap-written paths, not unrelated dirty state): (i) capture `ORIGINAL_BRANCH=$(git -C <target> rev-parse --abbrev-ref HEAD)` + `ORIGINAL_SHA=$(git -C <target> rev-parse HEAD)` + `git status --short` (closes Codex iter-4 #2 — live target is currently on `customer-cases-redial-linking-v1`, NOT `main`; capturing the actual original branch is required for rollback); (ii) require clean `git status --short` OR explicit owner waiver naming the unrelated dirty paths; (iii) create a fresh `feat/pr7-trial-adoption` branch in the target FROM `$ORIGINAL_SHA` (`git -C <target> checkout -b feat/pr7-trial-adoption "$ORIGINAL_SHA"` — closes Codex Tier-2 PR #16 #13: previous wording had bare `ORIGINAL_SHA` literal without `$` expansion, so verbatim execution would try to check out a non-existent ref. Bucket E already had the shell-expanded form `"$ORIGINAL_SHA"`; Sequencing now matches); (iv) record the planned-files list. **Rehearsal-on-MINIMAL-COPY BEFORE live apply** (closes Codex iter-3 #1 + self-check iter-3.5 #1 + Codex iter-4 #1 — earlier `cp -a` would copy 14GB of `data/` + 1.3GB `.venv/` + `secrets/` from call-details into `/tmp/`, a privacy + disk catastrophe): build a minimal-copy fixture via `mktemp -d` containing ONLY (i) the 4 collision-file contents (`CLAUDE.md`, `pyproject.toml`, `.gitignore`, `.python-version`) + (ii) needed parent directories (`src/`, `tests/`, `docs/plans/`, `.github/workflows/`). Use a small helper script in the trial-execution doc; explicit `trap` cleanup on exit. Run apply-against-minimal-copy + restore-against-minimal-copy to verify restore semantics end-to-end without touching the live target OR copying customer-data / secrets. **Rehearsal-decision-script** (closes Codex iter-4 #3 — `CLAUDE.md` and `pyproject.toml` are `manual_review_needed=true` per Scope #5 rules f/g; raw `--auto-accept-recommendations` would NOT auto-accept those, so a bare-stdin rehearsal apply would hang or exit 2 without recorded decisions): record the rehearsal decisions in `docs/trial-report-pr7.md` Section (b) — for each `manual_review_needed=true` collision, pre-decide the policy (e.g. CLAUDE.md → WRITE_NEW; pyproject.toml → SKIP). Pipe the decisions to stdin during rehearsal apply via heredoc. **Live apply MUST use the same decisions** as rehearsal (verified by re-reading Section (b) before live apply); any diverging live decision → restart rehearsal with the new decisions, do NOT skip the rehearsal-validation step. **Live apply runs AFTER rehearsal proves restore works**: `bootstrap.py --apply --mode=adopt --language python --project-name call-details --out ~/Desktop/Code/Boxette/call-details/` for real. Owner reviews each interactive prompt. After live apply: record BOTH `git -C <target> diff --stat -- <planned-files> <manifest-target-paths>` (tracked changes) AND `git -C <target> status --short -- <manifest-target-paths>` (untracked `.new` files from WRITE_NEW) per Bucket E Phase D step (3) — closes Codex Tier-2 PR #16 #15: previous wording had stale `git diff --stat HEAD~0` which omits untracked WRITE_NEW outputs and would undercount mutations. **Canonical executable recipe lives in Bucket E Phase D row** (preflight + rehearsal + live apply + rollback); this Sequencing entry is the conceptual narrative — any future command changes happen in Bucket E first, then mirror here. Live manifest parsed/recorded for rollback-readiness (NOT re-run for verification — rehearsal already validated). Document everything in `docs/trial-report-pr7.md`.
5. **Phase E — Trial-surfaced docs + BACKLOG only** (closes Codex iter-1 #7 — earlier "small fixes / docs improvements / BACKLOG entries" was open-ended scope creep; this PR already changes CLI grammar + manifest format + interactive UX + restore behaviour + tests + docs + live-repo trial). Phase E is **limited to docs additions and BACKLOG entries**. Any code polish that the trial surfaces becomes a follow-up PR unless it blocks the adoption-mode acceptance gates (in which case it folds into a Phase B/C iteration before that gate).

### Pre-loop user-decided scope constraints (before iter-1)

- **New flag is `--mode=adopt`** (closes Codex iter-1 #1 — earlier draft was contradictory: claimed `--mode=adopt` both joined the existing mutually-exclusive `--dry-run`/`--diff`/`--apply` group AND required explicit `--apply --mode=adopt`). Resolution: `--mode=adopt` is an **adoption modifier** that REQUIRES explicit `--apply` (write safety — never auto-implies an apply); is **invalid** with `--dry-run`, `--diff`, or `--restore`. NOT a fourth element of the mode-group. Use `--diff` (PR #6 contract) for read-only inspection; `--apply --mode=adopt` for the per-file analyze-then-decide-with-owner write.
- **Adoption-mode replaces collision-abort for `--apply` when `--mode=adopt` is set** — explicit opt-in; the existing collision-abort behaviour stays the default for unset `--mode=adopt`. `--overwrite-existing` is still a separate escape hatch for nuclear-override use cases.
- **Trial target is `~/Desktop/Code/Boxette/call-details/` exactly** (no fixture copy) — the empirical data is most valuable from the real target. Use `--dry-run` / `--diff` extensively before any `--apply --mode=adopt`; the analyze-phase's recommendations get owner confirmation before any write.
- **Trial-report artifact is `docs/trial-report-pr7.md`** (NOT `docs/lessons.md` — that name collides with the live `LESSONS.md`; renamed per Codex iter-3 #3 fold during the Plan PR #6 loop). The trial-report is a frozen snapshot of "how the skill behaved during this trial"; LESSONS.md is the ongoing per-session append-only log.
- **NO uv-binary version pin in CI** — PR #6's pre-loop user decision stays; PR #7 doesn't revisit.
- **NO migration tool (pip → uv)** — separately parked in BACKLOG (b) from PR #6.

## Scope

### IN scope

| # | Change | Where |
|---|---|---|
| 1 | New CLI flag `--mode=adopt` (closes Codex iter-1 #1 — resolves the contradictory framing in the iter-1 draft). **`--mode=adopt` is an adoption modifier, NOT a fourth mode in the mutually-exclusive `--dry-run`/`--diff`/`--apply` group.** Help text: `"Adoption modifier. Requires --apply. Enables per-file analyze-then-decide-with-owner UX for safe adoption into existing projects (see docs/usage.md). Invalid with --dry-run, --diff, --restore. For read-only inspection, use --diff."` Validation in `cli.py`: `--mode=adopt` without `--apply` → CLIError; `--mode=adopt` with `--dry-run`/`--diff`/`--restore` → CLIError; **`--mode=adopt` with `--language` != `python` → CLIError** (closes Codex iter-7 #6 — NOT-in-scope says "no `--mode=adopt` for Node or Go" but the existing validation didn't enforce this; add explicit `--language=python` requirement + tests); **`--mode=adopt` with `--overwrite-existing` → CLIError** (closes Codex iter-7 #7 — adopt-mode's per-file consent IS the consent model; `--overwrite-existing` is the nuclear escape hatch for plain `--apply`; combining them creates conflicting consent paths). ALSO add `--auto-accept-recommendations` (bool flag, only meaningful with `--mode=adopt`) + `--non-interactive` (bool flag, only meaningful with `--mode=adopt`). **Both flags can be combined** (closes Claude iter-2 #2 — earlier framing said mutually exclusive AND said "auto-accept fails with exit 2 under non-interactive"; only one could be true). Combined semantics: `--auto-accept-recommendations` accepts every policy with `manual_review_needed=false`; policies with `manual_review_needed=true` still need a prompt; under `--non-interactive` those prompts become exit-2 failures (the natural CI contract: "accept everything safe, fail on anything that needs human review"). | `bootstrap_lib/_flags.py` |
| 2 | New module `bootstrap_lib/adopt.py` — the analyze + recommend engine. Functions: `analyze_target(target_root, planned_files) -> AdoptionPlan`; `recommend_policy(rel_path, target_content, skill_content) -> PolicyRecommendation`; `format_recommendation_report(plan) -> str`. | `bootstrap_lib/adopt.py` (NEW) |
| 3 | Per-file `PolicyRecommendation` data class: `policy: Literal["WRITE","SKIP","OVERWRITE","WRITE_NEW","APPEND_MERGE"]`; `reason: str`; `confidence: Literal["high","medium","low"]`; `manual_review_needed: bool`. NamedTuple via class-syntax (per PR #6 Claude iter-2 #2 lesson). | `bootstrap_lib/adopt.py` |
| 4 | `AdoptionPlan` data class — wraps `PolicyRecommendation`s + the analyze-phase metadata (per-file: target file existence, size, sha256 of existing content, recognized "shape" markers). | `bootstrap_lib/adopt.py` |
| 5 | Recommendation heuristics (content-driven; informed by Phase A empirical data above — call-details collision set is `{.gitignore, .python-version, CLAUDE.md, pyproject.toml}`). **WRITE = create where nothing existed; OVERWRITE = replace existing content (captures `content_before` for SHA-guarded restore); SKIP = leave the file alone with no manifest entry** (closes Codex Tier-2 PR #16 #5 — earlier rules (b)/(c)/(e) all assigned `WRITE` to existing files, but Bucket B's v2 restore matrix defines `WRITE` as a created-file policy that restores by **deleting** the path; classifying an existing file as `WRITE` would make `--restore` delete a pre-existing user file, violating the safety contract). Rules (evaluated in order; first match wins): **(a0) planned CREATE is `ignored_by_git` (target file doesn't exist AND `git check-ignore -v` matches) → `SKIP` by default OR `WRITE_NEW` with `manual_review_needed=true` if the owner wants to write the ignored file anyway** (closes Codex Tier-2 PR #16 #7 — iter-7 #1 added `TargetMeta.ignored_by_git` + described the recommend_policy contract in Bucket B but never added the precedence rule to the Scope #5 rule list, so a missing ignored file like `AGENTS.md` would fall through to rule (a) → `WRITE` → file silently written + invisible to `git diff --stat` since `.gitignore` excludes it; this rule fires BEFORE rule (a) so the precedence is explicit and tested); (a) target file doesn't exist AND NOT `ignored_by_git` → `WRITE` (create; restore deletes); (b) target file is empty / whitespace-only → `OVERWRITE` (file exists with empty content; capture empty `content_before`; restore writes empty back); (c) target content matches skill's template render byte-for-byte → `SKIP` (no write, no manifest entry; file already matches — semantically honest no-op rather than a "WRITE no-op"); (d) target file is `.gitignore` AND skill's patterns are NOT all already present → `APPEND_MERGE` with `manual_review_needed=false` (line-level idempotent: only append patterns missing from the target — closes Codex iter-1 #5; if all patterns already present → `SKIP` "already covered"); (e) target file is `.python-version` AND target's Python-version matches skill's `python_version` (3.12) → `SKIP` (byte-identical; nothing to write, nothing to restore); else `SKIP` with note "target pins a different Python version"; (f) target file is `CLAUDE.md`/`AGENTS.md`/`CONTRIBUTING.md`/`BACKLOG.md`/`LESSONS.md` AND non-trivial (>20 lines OR contains custom section headings not in skill template) → `WRITE_NEW` with `manual_review_needed=true` ("preserve target's domain content; merge manually with `diff -u <orig> <orig>.new`"); (g) target file is `pyproject.toml` AND non-trivial (has `[project] dependencies`, `[tool.*]`, or `[dependency-groups]` table) → `SKIP` with `manual_review_needed=true` ("project's pyproject.toml is the source of truth for deps; review the diff manually with `bootstrap.py --diff` for any worth-adopting fields"); **(h) DEFAULT for any existing non-empty file not matching rules (a)-(g) → `SKIP` with `manual_review_needed=true`** (closes Codex iter-1 #3 — earlier draft allowed unknown existing files to fall through to destructive WRITE; this is the core safety guarantee of adoption-mode and the reason `--mode=adopt` is safer than `--overwrite-existing`). `--auto-accept-recommendations` accepts every policy with `manual_review_needed=false` without a prompt; policies with `manual_review_needed=true` still require a prompt. Under `--non-interactive` (which can be combined with `--auto-accept-recommendations`), `manual_review_needed=true` policies become exit-2 failures rather than prompts — the natural CI contract. | `bootstrap_lib/adopt.py` |
| 6 | Interactive decide-phase UX: print the recommendation report (per-file rows), then **for each conflicted file with `manual_review_needed=true`** ask the prompt with a **per-file allowed-actions matrix** (closes Codex iter-5 #3 — earlier blanket `[o]verwrite / [a]ppend` for every manual-review file would have let users APPEND_MERGE pyproject.toml or OVERWRITE CLAUDE.md, both of which can corrupt high-value files; APPEND_MERGE is only valid for `.gitignore` per Scope #5 rule (d) + Architecture decision). Action availability per file: (a) **always** `[r]ecommended` (default) + `[s]kip` + `[d]iff` + `[?]help` + `[q]uit`; (b) `[n]ew` (WRITE_NEW) — available for any manual-review file; (c) **`[a]ppend` (APPEND_MERGE) — ONLY available for `.gitignore`**; (d) **`[o]verwrite` — only after typed `OVERWRITE` (uppercase) confirmation** (single-keystroke `o` won't suffice; non-developer-audience safety against a stray keystroke destroying CLAUDE.md). Unavailable actions are not shown in the prompt for that file. **Single-matrix contract**: (i) auto-accept applies every `manual_review_needed=false` policy without a prompt; (ii) `manual_review_needed=true` policies always need a decision — `--auto-accept-recommendations` does NOT auto-accept these (they still go to interactive prompt OR exit-2 per non-interactive); (iii) prompts read from stdin (heredoc / pipe input works — closes Codex iter-5 #1); EOF on stdin before a required decision → exit-2; (iv) `--non-interactive` forces exit-2 for any prompt that would fire (no fallback). | `bootstrap_lib/cli.py` + `bootstrap_lib/adopt.py` |
| 7 | Apply-phase wiring: when `--mode=adopt`, **new functions** `plan_adoption_entries(target_root, planned_files, adoption_plan) -> entries` (in `manifest.py` — closes Codex iter-7 #3 — must take `planned_files` to compute after-SHA values for WRITE/WRITE_NEW/APPEND_MERGE; can't be reconstructed from `target_root` + `adoption_plan` alone since `TargetMeta` deliberately excludes raw content) + `_apply_adoption_writes(root, planned_files, adoption_plan, entries) -> None` (in `cli.py`) replace the existing `manifest.plan_entries` + `_apply_writes` flow (closes Codex iter-5 #2 — existing `_apply_writes` writes every `planned_files` entry to `root/rel_path`, doesn't know SKIP / WRITE_NEW / APPEND_MERGE; existing `manifest.plan_entries` treats existing target as plain overwrite). Per-policy write contract (closes Codex iter-6 #3 — SKIP entries are NOT in restore manifest; restore manifest is mutation-only): `SKIP` → no write, **NO manifest entry**; SKIP decisions are recorded in the adoption report + trial report (Section (b)), NOT in the restore manifest. `WRITE` → write `root/rel_path`, manifest entry with `policy=WRITE`; `OVERWRITE` → write `root/rel_path` + v1-style before-snapshot, manifest entry with `policy=OVERWRITE`; `WRITE_NEW` → write `root/<rel_path>.new`, manifest entry with `policy=WRITE_NEW` and `target_path=<rel_path>.new`; `APPEND_MERGE` → atomically write `root/rel_path` with only missing lines appended, manifest entry with `policy=APPEND_MERGE`, `pre_append_length`, `target_path=<rel_path>`. New restore-manifest entry types for `.new` writes and `APPEND_MERGE`. **`.new` collision rule** (closes Codex iter-1 #5 — earlier draft didn't say what happens if `<original>.new` already exists): if `<original>.new` exists at apply time, fail-loud with exit 2 + clear message ("`<original>.new` already exists — rename or remove it before running `--mode=adopt`; bootstrap will NOT overwrite an existing `.new` file"). Alternative (`<original>.new.<N>` rotation) parked as a future BACKLOG entry — fail-loud is safer for the non-developer audience's first encounter. **`APPEND_MERGE` semantics**: read target file line-by-line; for each line in skill's content, append ONLY if the line is not already present in target (set-comparison after stripping); preserves order of new additions; idempotent — running twice produces same result. Restore semantics: `.new` file is removed; append-merge restores by truncating target to the captured pre-append length. | `bootstrap_lib/cli.py` + `bootstrap_lib/manifest.py` |
| 8 | `--diff` mode extension: per-file `policy` annotation in the unified-diff header (e.g. `--- a/CLAUDE.md (target: ~96 lines, domain-rich)` / `+++ b/CLAUDE.md (recommendation: WRITE_NEW)` — line count is illustrative; actual values come from runtime analysis, not a plan-frozen number; closes self-check iter-5.5 #2) so the user sees the recommendation alongside the diff before running `--apply --mode=adopt`. **Plain `--diff` runs the analyzer in read-only mode for `--language=python` automatically** (closes Codex iter-3 #6 — `--diff` is invalid with `--mode=adopt`, but `--diff` SHOULD still show the policy annotations because that's the read-only-inspection path users follow before running adopt). No manifest, no writes. For non-Python languages, `--diff` keeps the existing PR #1 behaviour (no policy annotations — adopt-mode is Python-only). Test: a synthetic call-details-shaped fixture under `--diff --language python` produces diffs with policy headers in the output AND creates no manifest file in `$TMPDIR`. | `bootstrap_lib/cli.py` |
| 9 | Tests: unit tests for `analyze_target` + `recommend_policy` (rule-by-rule); integration tests for `--mode=adopt` end-to-end against synthetic fixtures matching call-details collision shapes; **interactive-prompt test via stdin-injected synthetic input** (NOT PTY mock — closes Claude iter-2 #8; PTY mocking varies across CI runners, especially macOS; stdin injection is portable) for the cases auto-accept can't cover (manual-review prompts); `--non-interactive` failure path test | `tests/` (NEW: `test_adopt_engine.py`, `test_mode_adopt_smoke.py`; extensions to `test_bootstrap_cli.py`) |
| 10 | Active-docs updates: `README.md` "Status" + "Build sequence" (PR #7 ✅); `SKILL.md` invocation block + adoption-mode paragraph; `docs/usage.md` new "Adoption mode (`--mode=adopt`)" section covering analyze → recommend → decide → apply with a worked example based on call-details; `BACKLOG.md` close PR #7 entries + add PR #7 follow-ups. | `README.md`, `SKILL.md`, `docs/usage.md`, `BACKLOG.md` |
| 11 | NEW: `docs/trial-report-pr7.md` — one-time structured trial-experience write-up. Sections: (a) Trial target shape (call-details file inventory + collision set — counts + filenames + sha256s ONLY, no content excerpts); (b) Phase A empirical findings + rehearsal-decision-script (per-file: target size + sha256, skill template sha256, recommended policy, **owner-pre-decided rehearsal policy** for `manual_review_needed=true` collisions — closes self-check iter-4.5 #2; the decisions piped via heredoc during minimal-copy rehearsal apply per Sequencing Phase D), reasoning; (c) Phase B validation results — heuristic-by-heuristic, did each Scope #5 rule fire correctly against the call-details collision set? Heuristics are LOCKED at iter-2 (closes self-check iter-3.5 #3 — earlier "what the trial changed" wording contradicted the lock). **Misfire handling follows the misfire-split rule (canonical: Sequencing Phase B + Risks row + Bucket E Phase A — closes Codex Tier-2 PR #16 #11 audit pass)**: `manual_review_needed=true` misfires logged as observations + UX gap in Section (e); `manual_review_needed=false` misfires trigger in-PR loop-back to Phase C and are recorded here ALONG WITH the resulting rule fix (file: change-pair = misfire observation + applied rule change in Section (c)); (d) Phase D apply result (was `call-details/` better off after? was anything broken? — branch SHAs + `git diff --stat` summary + rehearsal restore-round-trip result, NOT diff content) (closes self-check iter-3.5 #4 — Bucket E added "UX gaps?" + "rehearsal result" items here; rehearsal result correctly belongs in (d), UX gaps move to (e)); (e) Skill UX gaps surfaced (what was confusing for a non-developer; recommendation-engine misfires from Phase B observations); (f) Recommendations for future skill polish (BACKLOG additions). **Privacy boundary** (closes Codex iter-1 #6 — call-details contains `secrets/`, `data/`, customer domain content; the skill repo will eventually be public): NO secrets, NO transcript / customer / PII data, NO raw file excerpts. Safe content: filenames, file sizes, sha256 hashes, structural categories ("X-line file with Y section headings"), policy decisions + reasoning. If in doubt about a section: summarize without quoting. NOT the same as ongoing `LESSONS.md`. | `docs/trial-report-pr7.md` (NEW) |
| 12 | LESSONS.md append (impl-session) for any new mistake-class surfaced during PR #7 work | `LESSONS.md` |

### NOT in scope

- **No `--mode=replace` / `--mode=overwrite` aliases.** `--overwrite-existing` already exists for that purpose; not bundling a renaming-bikeshed into PR #7.
- **No per-file policy table in CLI args** (e.g. `--policy CLAUDE.md=skip,Makefile=write`). Per user 2026-05-19: content-driven analysis + interactive decide-phase, NOT a flag soup.
- **No automatic content merging for `pyproject.toml` deps** (e.g. union user-deps + skill-deps). Adoption-mode recommends SKIP for pyproject — user reviews the diff manually. Auto-merge of TOML structure is a future BACKLOG entry.
- **No template-vs-existing semantic understanding for arbitrary files.** Heuristics use file-name + size + sha256 + shape markers; no LLM-call inside the bootstrap engine.
- **No retroactive adoption mode for PR #1–#6 generated projects.** Adoption-mode is a forward-looking adoption-time tool, not a re-adoption / sync tool.
- **No undo/redo within an interactive session.** If user picks the wrong policy, they `q`uit, fix their thinking, re-run. Restore manifest still works after the fact.
- **No `--mode=adopt` for Node or Go.** PR #7 trial uses a Python target; adoption-mode is python-first for now. Same-language-extension is parked for follow-up.

## Subsystem breakdown

### Bucket A — CLI flag + plumbing

| File | Change |
|---|---|
| `bootstrap_lib/_flags.py` | Add `--mode=adopt` as a SINGLE-VALUE adoption modifier flag (closes Codex iter-1 #1 + self-check iter-1.5 #1 — NOT a four-value mutually-exclusive group; `--mode` only accepts the literal string `adopt` since that's the only modifier we need). Argparse: `parser.add_argument("--mode", choices=["adopt"], default=None)`. Help text: `"Adoption modifier. Requires --apply. Enables per-file analyze-then-decide-with-owner UX for safe adoption into existing projects (see docs/usage.md). Invalid with --dry-run, --diff, --restore. For read-only inspection, use --diff."` (matches Scope #1's canonical wording — closes self-check iter-2.5 #4). ALSO add `--auto-accept-recommendations` (bool flag, only meaningful with `--mode=adopt`) + `--non-interactive` (bool flag, only meaningful with `--mode=adopt`; **CAN be combined with --auto-accept-recommendations** — closes self-check iter-2.5 #1; combined semantics per Scope #1 + iter-2 #2 fold: auto-accept handles `manual_review_needed=false` policies; non-interactive turns `manual_review_needed=true` prompts into exit-2 failures). |
| `bootstrap_lib/cli.py` | `_resolve_mode` is **unchanged for the existing 4 modes** (dry-run / diff / apply / restore — closes Claude iter-2 #7 off-by-one in the iter-2 draft). Adopt-mode wires up the NEW write path (closes self-check iter-5.5 #3 + Codex iter-6 #2 — earlier wording mentioning both function names was parseable two ways): when `args.mode == "adopt"`, `main()` AFTER mode resolution + path-safety calls `adopt.analyze_target` → `_interactive_decide` → `manifest.plan_adoption_entries` → `_apply_adoption_writes` (closes self-check iter-6.5 #1 — earlier fold left a duplicate "`_apply_writes` per the agreed policies" sentence dangling from the iter-5 #2 rewrite; removed). The legacy `_apply_writes` function is NOT invoked in adopt-mode under any circumstances; it stays in place only for the existing non-adopt apply path (plain `--apply`). New post-resolve check: if `--mode=adopt` set, validate `--apply` is also set and `--dry-run` / `--diff` / `--restore` are NOT set; else CLIError. Restore-mode invalid-flag list extends with `--mode`, `--auto-accept-recommendations`, `--non-interactive`. |

### Bucket B — Adoption engine

| File | Change |
|---|---|
| `bootstrap_lib/adopt.py` (NEW) | The full analyze-then-recommend engine. ~250-400 lines. Functions: `analyze_target(target_root: Path, planned_files: dict[str, bytes]) -> AdoptionPlan`; `recommend_policy(rel_path: str, target_path: Path, skill_content: bytes, target_meta: TargetMeta) -> PolicyRecommendation`; `format_recommendation_report(plan: AdoptionPlan) -> str` (the user-facing report). Data classes: `PolicyRecommendation`, `AdoptionPlan`, `TargetMeta` (file shape: `exists: bool`, `size: int`, `sha256: str | None`, `line_count: int | None`, `heading_count: int | None` (for markdown files; count of `^#` lines), `has_dependency_groups: bool` (for pyproject.toml; True if `[dependency-groups]` table present), `python_version_pin: str | None` (for `.python-version`), `ignored_by_git: str | None` (closes Codex iter-7 #1 — gitignore-aware analysis for planned CREATE files; value is the matching `.gitignore` line from `git check-ignore -v` output, or None if the file would commit normally; recommend_policy uses this: if a missing planned file is `ignored_by_git is not None`, recommend `SKIP` (default) OR `WRITE_NEW` with `manual_review_needed=true` so the interactive prompt asks the owner)). **NO `content_sample` field** (closes Codex iter-3 #4 — earlier draft had `content_sample` which is exactly the kind of field that leaks raw target content into reports/logs, contradicting Scope #11's privacy boundary). All NamedTuple via class-syntax. Test row asserts the user-facing `format_recommendation_report` output contains no raw target content (only derived markers, sha256s, decisions). |
| `bootstrap_lib/manifest.py` | Add `format_version: int` field at top level of `Manifest.to_dict()` (introduces the field; not previously present per Claude iter-2 #3). New entry-level fields: `policy: str` (one of WRITE/SKIP/OVERWRITE/WRITE_NEW/APPEND_MERGE) + `target_path: str` (for WRITE_NEW: where the .new file goes — usually `<path>.new`; for APPEND_MERGE: where the appended-to original is — usually `<path>`; for OVERWRITE / WRITE: same as `path`; **SKIP has no manifest entry so no `target_path` value applies** — closes Codex Tier-2 PR #16 #2; the iter-6 #3 "restore manifest is mutation-only" fold updated the per-policy matrix but left the field-definition clause stale) + `pre_append_length: int | None` (only for APPEND_MERGE; absent / null for other policies) + `sha256_before_target_path: str | None` + `sha256_after_target_path: str | None` (closes Codex iter-4 #4 — earlier draft mentioned `sha256_before_target_path` in restore semantics but didn't list it in the v2 fields). Per-policy definition: for WRITE_NEW, `target_path=<path>.new`, `sha256_before_target_path=None` (the .new file didn't exist pre-apply), `sha256_after_target_path = sha256(rendered_content)`; for APPEND_MERGE, `target_path=<path>`, `sha256_before_target_path = sha256(pre-append target)`, `sha256_after_target_path = sha256(post-append target)`, `pre_append_length = len(pre-append target bytes)`; for SKIP, **no manifest entry exists at all** (closes Codex iter-6 #3 — restore manifest is mutation-only; SKIP decisions live in the adoption report + `docs/trial-report-pr7.md` Section (b), not the manifest). Missing-file behaviour for WRITE_NEW restore: if `target_path` (the `.new` file) doesn't exist at restore time → no-op + warning (user already removed it). **Backward-compat dispatch**: `Manifest.from_dict` first reads `data.get("format_version")`; if `None` or absent → v1 path (legacy restore semantics, no policy field expected on entries — preserves PR #1–#6 manifest compat); if `2` → v2 path (new policy-aware semantics). **v2 restore semantics — SHA-GUARDED full matrix for every mutating policy** (closes Codex iter-3 #2 + iter-7 #2 — earlier draft only specified WRITE_NEW + APPEND_MERGE; WRITE + OVERWRITE need their own row each since they're load-bearing safety contract): v2 entries retain v1's `existed_before`, `content_before_b64` (None for created files), `mode_before` / `mode_after`, `sha256_before` / `sha256_after` fields (the legacy fields keyed on `path`); v2 ADDS `policy`, `target_path`, `sha256_before_target_path`, `sha256_after_target_path`, `pre_append_length`. **Restore matrix per policy**: (a) `WRITE` (created file, didn't exist before): if current_sha(path) == `sha256_after` → remove file + remove parent dirs in reverse-depth order if empty; else (interrupted or user-edited) → no-op + warning. (b) `OVERWRITE` (modified existing file): if current_sha(path) == `sha256_after` → write `content_before_b64` decoded back + restore `mode_before`; matches `sha256_before` → no-op (apply was interrupted before this entry); else → SKIP with warning. (c) `WRITE_NEW` (created `.new` file next to original; original untouched): SHA-check `target_path` (the `.new` file), not `path`; if current_sha(target_path) == `sha256_after_target_path` → remove `.new` file; matches `sha256_before_target_path` (None for WRITE_NEW since `.new` didn't exist) → no-op; else → SKIP with warning. (d) `APPEND_MERGE` (modified original by append-only): SHA-check `target_path` (the original); if current_sha == `sha256_after_target_path` → truncate file to `pre_append_length` bytes; matches `sha256_before_target_path` → no-op; else → SKIP with warning. (e) `SKIP` policy → NOT in manifest at all (closes Codex iter-6 #3); nothing to restore. Path-safety validation on `target_path` before any mutation. |
| `bootstrap_lib/render.py` | No changes (render is mode-agnostic — produces the planned_files dict; adopt.py consumes it). |

### Bucket C — Interactive decide-phase UX

| File | Change |
|---|---|
| `bootstrap_lib/cli.py` | New `_interactive_decide(plan: AdoptionPlan) -> AdoptionPlan` function. **Matches Scope #6's single-matrix contract** (closes Codex iter-3 #3): only prompts for files with `manual_review_needed=true` AND not under `--non-interactive`; under `--auto-accept-recommendations`, `manual_review_needed=false` policies skip the prompt (auto-applied); `manual_review_needed=true` policies still prompt; EOF-on-stdin or `--non-interactive` → exit 2. Uses stdin (no prompt-toolkit dep — non-developer audience shouldn't need extra installs). |
| `docs/usage.md` | Document the prompt UX with example output |

### Bucket D — Tests

| File | Asserts |
|---|---|
| NEW: `tests/test_adopt_engine.py` | Unit tests for `recommend_policy` rule-by-rule (one test per heuristic rule a-h above). **Default-rule test (rule h)**: existing non-empty file not matching rules (a)-(g) → recommendation is `SKIP` with `manual_review_needed=true` (closes self-check iter-1.5 #3 — earlier wording said "files that should fall through to default WRITE", which was the iter-1 #3 safety bug that fold P3 fixed in the heuristic but not in the test wording). Plus: `analyze_target` tests with synthetic file fixtures. `format_recommendation_report` golden-output test (assert structure, not byte-equal). |
| NEW: `tests/test_mode_adopt_smoke.py` | **Two fixtures** (closes Codex iter-6 #5 — single auto-accept-only smoke would hang or EOF on `manual_review_needed=true` collisions): (i) **all-safe fixture** — only `manual_review_needed=false` collisions: byte-identical `.python-version` → `SKIP` per rule (e), `.gitignore` with **some skill patterns MISSING** → `APPEND_MERGE` per rule (d) (closes Codex Tier-2 PR #16 #6 — earlier wording said `.gitignore` is "all-skill-patterns-present → SKIP" AND that the fixture verifies APPEND_MERGE, but rule (d) makes those mutually exclusive; APPEND_MERGE is `manual_review_needed=false` so it belongs in the auto-accept smoke and the fixture must actually trigger it), empty `requirements-dev.txt` → `OVERWRITE` per rule (b), an additional byte-identical file (e.g. matching `pyproject.toml`-shaped template) → `SKIP` per rule (c) (closes Codex Tier-2 PR #16 #5 — earlier wording said this fixture verifies "WRITE / APPEND_MERGE-no-op / SKIP-already-covered" but rules (b)/(c)/(e) are now `OVERWRITE`/`SKIP`/`SKIP` respectively, not `WRITE`); `--mode=adopt --auto-accept-recommendations` runs end-to-end without prompts; verifies `OVERWRITE`-from-empty / `APPEND_MERGE`-real-fire (not no-op) / `SKIP`-byte-identical cases. (ii) **call-details-shaped fixture** — full 4 collisions including `CLAUDE.md` + `pyproject.toml` (both `manual_review_needed=true`); test pipes a recorded stdin decision script (heredoc) for the manual-review prompts; verifies the full Scope #5 rule set (`.gitignore` → `APPEND_MERGE`; `.python-version` → `SKIP` if version matches per rule (e), else `SKIP` per the "different version" branch; `CLAUDE.md` → `WRITE_NEW`; `pyproject.toml` → `SKIP`; 15 missing-target files → `WRITE` per rule (a) — these are CREATEs, not modifications). Both fixtures verify: collision-abort does NOT fire; restore manifest captures policies (SKIP entries NOT in manifest per iter-6 #3); restore works for each policy AND `--restore` does NOT delete pre-existing files classified as `OVERWRITE`/`SKIP` (closes Codex Tier-2 PR #16 #5 — explicit assertion that the safety-contract regression is tested). |
| `tests/test_bootstrap_cli.py` (extended) | `--mode=adopt` valid + invalid combinations (`--mode=adopt` requires `--apply`; invalid with `--dry-run`/`--diff`/`--restore`); `--mode=adopt` in restore mode → error; **CI-safety matrix** (closes Claude iter-2 #2): `--non-interactive` alone, conflicts include `manual_review_needed=true` → exit 2; `--auto-accept-recommendations` alone, conflicts include `manual_review_needed=true` → falls back to interactive prompt (test injects synthetic stdin); `--auto-accept-recommendations` + `--non-interactive` combined, conflicts include `manual_review_needed=true` → exit 2 (the natural CI contract); `--auto-accept-recommendations` + `--non-interactive`, all policies `manual_review_needed=false` → applies cleanly with no prompts. |
| `tests/test_manifest.py` (extended) | New policy fields serialize/deserialize cleanly; restore handles each policy type |

### Bucket E — Trial execution

**This bucket is unique to PR #7 — it's the trial, not test code.** Phase A runs after PR #7 plan converges + Phase A subagent task. Phase D runs after PR #7 impl ships.

| Phase | Activity | Deliverable |
|---|---|---|
| A (entirely **post-implementation gate** — closes Codex iter-5 #4: earlier wording said "remainder runs after plan converges" which implied pre-implementation; aligning to Verification gate #8 which lists Phase A under Phase 2 post-implementation) | Run `bootstrap.py --dry-run --language python --project-name call-details --out ~/Desktop/Code/Boxette/call-details/` (PR #6 contract) — this was run during iter-1 fold to anchor the collision baseline in Context, but the canonical Phase A execution happens AFTER impl PR opens. Run `--diff` (same flags) for each of the 4 collisions; capture file sizes, sha256 hashes, structural categories (NOT content excerpts — per privacy boundary in Scope #11). | Empirical findings dataset → `docs/trial-report-pr7.md` Section (a) + (b). **Heuristics are LOCKED at iter-2** (closes self-check iter-2.5 #3 + iter-2 #4): Phase A is **post-implementation validation only**. **Misfire remediation follows the misfire-split rule** (closes Codex Tier-2 PR #16 #11 — earlier "remediation is a follow-up PR, NOT a rewrite" wording contradicted Phase B + Risks-row in-PR loop-back rule for safe-policy misfires; canonical rule lives in Sequencing Phase B): (1) `manual_review_needed=true` misfire → UX gap in trial-report Section (e); follow-up PR optional. (2) `manual_review_needed=false` misfire → in-PR loop-back to Phase C per Sequencing Phase B (Phase A surfaces the empirical data; Phase B is the gate; Phase C is the fix location). |
| D (post-impl) | **Preflight + Rehearsal-on-MINIMAL-copy + Live apply + Rollback recipe** — matches Sequencing Phase D wording exactly (closes self-check iter-4.5 #1 — Bucket E still had stale `cp -a` of 14GB+ live target despite iter-4 #1 fold in Sequencing; same drift class as iter-1.5/2.5/3.5 #1). **Active executable recipe** (closes Codex iter-6 #6 — earlier wording deferred to Sequencing Phase D for the recipe; making Bucket E the canonical executable place since impl reads this directly): **(1) Preflight**: `ORIGINAL_BRANCH=$(git -C <target> rev-parse --abbrev-ref HEAD)`; `ORIGINAL_SHA=$(git -C <target> rev-parse HEAD)`; require clean `git -C <target> status --short` OR explicit owner waiver naming dirty paths; `git -C <target> checkout -b feat/pr7-trial-adoption "$ORIGINAL_SHA"`; **Phase A `git check-ignore -v --` against every planned-CREATE path** (closes Codex iter-6 #4 — AGENTS.md verified gitignored in target); record owner decisions for any matches. **(2) Rehearsal-on-minimal-copy**: `REHEARSAL_DIR=$(mktemp -d)`; `trap 'rm -rf "$REHEARSAL_DIR"' EXIT`; `cp -a <target>/{CLAUDE.md,pyproject.toml,.gitignore,.python-version} "$REHEARSAL_DIR"/`; `mkdir -p "$REHEARSAL_DIR"/{src,tests,docs/plans,.github/workflows}`; `bootstrap.py --apply --mode=adopt --language python --project-name call-details --out "$REHEARSAL_DIR" <<< "$RECORDED_DECISIONS"` (heredoc for `CLAUDE.md` + `pyproject.toml` from trial-report Section (b)); `bootstrap.py --restore <rehearsal-manifest>` → assert restore matches pre-rehearsal state. **(3) Live apply**: same `--apply --mode=adopt` command against `<target>` with SAME decisions. **Record BOTH** (closes Codex Tier-2 PR #16 #12 — `git diff --stat` compares commit/index/worktree and doesn't surface untracked files like `<path>.new` from WRITE_NEW; using diff-stat alone undercounts the actual mutation surface): (i) `git -C <target> diff --stat -- <planned-files> <manifest-target-paths>` for tracked-file changes (additions to tracked files, APPEND_MERGE results, content changes); (ii) `git -C <target> status --short -- <manifest-target-paths>` to capture untracked `.new` files from WRITE_NEW entries. Where `<manifest-target-paths>` extracts every `target_path` from the live manifest's WRITE_NEW + APPEND_MERGE entries (closes Codex Tier-2 PR #16 #10 — adopt mode mutates `.new` files for rule (f)/(a0) WRITE_NEW decisions; those paths are NOT in the planned-files list, so a planned-files-only diff-stat would also undercount real mutations; the manifest IS the source of truth for what was mutated). Closes Codex iter-6 #6 — both commands stay scoped via the paths argument (NOT full repo state which would include unrelated dirty state); together they fully describe the trial's mutation surface. **(4) Rollback recipe** (executable if needed): (a) `bootstrap.py --restore <live-manifest>`; (b) `git -C <target> status` confirm restore matches preflight; (c) **Residual `.new` cleanup derived from manifest** (closes Codex Tier-2 PR #16 #14 — earlier wording hardcoded `{CLAUDE.md,pyproject.toml}.new` but rules (a0) and (f) can produce WRITE_NEW for any path including ignored creates and other domain-rich markdown files; manifest-derived cleanup covers every actual WRITE_NEW output AND survives a future rule that produces new `.new` targets without code changes here): extract every `target_path` ending in `.new` from the live manifest (`jq -r '.entries[] | select(.policy=="WRITE_NEW") | .target_path' <live-manifest>`) and `rm -f <target>/<path>` for each; (d) `git -C <target> diff` for APPEND_MERGE residue inspection; (e) `git -C <target> checkout "$ORIGINAL_BRANCH" && git -C <target> branch -D feat/pr7-trial-adoption`. | Trial-report Section (d) — was call-details/ better off after? Anything broken? Branch SHAs + diff-stat summary + rehearsal restore-round-trip result (NO diff content per privacy boundary; UX-gap items belong in Section (e), NOT (d), per Scope #11 — closes self-check iter-4.5 #3). |

### Bucket F — Active docs + LESSONS

| File | Change |
|---|---|
| `README.md` | Status section: PR #7 ✅; Build sequence: PR #7 ✅ hybrid trial + adoption-mode redesign; mention `--mode=adopt` in the feature list as "safe adoption into existing projects". |
| `SKILL.md` | Invocation block adds `[--mode adopt] [--auto-accept-recommendations] [--non-interactive]`. New short paragraph: "`--mode=adopt` requires `--apply` and is invalid with `--dry-run`/`--diff`/`--restore`. Enables per-file analyze-then-decide-with-owner UX for safe adoption into existing projects (see `docs/usage.md`)." **Existing safety-contract text edit** (closes Codex iter-4 #6 — currently SKILL.md says `--apply` aborts on existing files unless `--overwrite-existing` is passed; that wording stays accurate for plain `--apply` but is misleading when adopt-mode exists): update the existing collision-contract paragraph to say "**plain `--apply`** aborts on collisions unless `--overwrite-existing` is passed; **`--apply --mode=adopt`** uses per-file recommendations (analyze-then-decide-with-owner) — see adoption mode below". |
| `docs/usage.md` | New "Adoption mode (`--mode=adopt`)" section (~2 pages). Worked example based on `call-details/`: dry-run output, recommendation report, interactive prompt sequence, final apply, restore. **Existing Modes table edit** (closes Claude iter-2 #5): add a footnote/note immediately under the Modes table: "`--mode=adopt` is a per-file adoption modifier of `--apply` — NOT a 5th mode. See the Adoption mode section below." **Existing args-table `--overwrite-existing` row edit** (closes Codex iter-4 #6 — currently says "required during `--apply` if any target file already exists" without acknowledging adopt-mode as the safer alternative): append "; OR use `--apply --mode=adopt` for per-file recommendations instead of the nuclear overwrite". |
| `BACKLOG.md` | **Update the existing PR #7 entries (c), (d), (e) from PR #6** to mark them ✅ Done + correct the stale 8-collision baseline to the verified 4 MODIFY + 15 CREATE (closes Codex iter-3 #5 — `BACKLOG.md:456` still has the pre-empirical 8-collision count from PR #6's plan). Add PR #7 follow-ups (any surfaced by trial — likely: per-file diff annotations richer rendering, undo within interactive session, language extension to Node/Go, PTY-mock-based richer interactive testing). |
| `LESSONS.md` | Append entry (impl-session) for any new mistake-class. Already-known candidate: "Restore-manifest extensions for new policy types must preserve backward-compat with PR #6 manifests" (probable test surface). |

## Architecture decisions

- **`--mode=adopt` is opt-in, not a default.** Plain `--apply` keeps the existing PR #1 collision-abort contract. Backwards-compatible. Users adopt the new mode deliberately.
- **Analyze phase reads target files but writes NOTHING.** Pure analysis. The decide-phase is where the contract becomes mutable; apply-phase is where the writes happen.
- **Recommendation heuristics are content-driven, not policy-table-driven** (per user 2026-05-19). Heuristics inspect: file existence, size, sha256-vs-skill-template, recognized shape markers (frontmatter, section headings, code patterns). Different projects produce different recommendations.
- **Interactive decide UX uses stdin, not prompt-toolkit.** Keep the non-developer audience's prereqs minimal (no `pip install prompt-toolkit`). Plain readline-style prompts.
- **`.new` writes go alongside the original** (`CLAUDE.md.new` next to `CLAUDE.md`). User's existing file is untouched. Visual diff via the user's normal git/editor tooling.
- **APPEND_MERGE only for `.gitignore`** initially. Other files (e.g. `README.md` with a "Status" section append) are too risky for automated merging — recommend SKIP with manual diff.
- **The trial-report (`docs/trial-report-pr7.md`) is a one-time deliverable**, not a recurring artifact. PR #8+ won't add to it. Future trials get their own report.
- **Restore-manifest format-version introduces `format_version: 2`** for the new policy fields (closes Claude iter-2 #3 — PR #1–#6 manifests have NO `format_version` field at all; PR #7 is introducing the field, not bumping it). Backward-compat rule: `Manifest.from_dict` treats **missing or absent `format_version` as v1** (the PR #1–#6 shape) and `format_version == 2` as v2 (PR #7+ with the new `policy` field in entries). v1 manifests dispatch to the legacy restore path (no `policy` field in entries; original PR #1 semantics). Field name `format_version` (snake_case; matches existing `created_at` / `target_root` naming) — committed to this spelling because it will be in committed JSON forever.

## Risks + mitigations

| Risk | Mitigation |
|---|---|
| Interactive prompts hang on non-TTY stdin (CI runners) | **EOF is the failure condition, NOT non-TTY by itself** (closes Codex iter-5 #1 — earlier exit-2-on-non-TTY would break the heredoc rehearsal path that pipes pre-recorded decisions via stdin; heredoc inputs are non-TTY by definition but should succeed if they provide all decisions). Rules: (a) `--non-interactive` set AND a `manual_review_needed=true` decision is needed → exit 2 (regardless of TTY state); (b) stdin reads succeed normally (TTY or not — heredoc / pipe inputs work); (c) EOF reached before all required decisions answered → exit 2 with a clear message. The auto-accept × non-interactive × manual-review matrix from Bucket D tests all four combinations against fixture-injected pipe stdin. |
| Heuristics misfire on call-details — produce a recommendation set the user rejects | **Heuristics in Scope #5 are LOCKED at iter-2** (closes Claude iter-2 #4 — earlier draft was ambiguous about whether Phase A could rewrite them). Phase A is validation-only. **Remediation depends on which kind of recommendation misfired** (closes Codex Tier-2 PR #16 #9 — earlier "remediation is a follow-up PR" wording contradicted Phase B's round-4 in-PR-loop-back rule for safe-policy misfires): (1) **`manual_review_needed=true` misfire** — owner rejects via interactive prompt anyway (the prompt IS the mitigation); UX gap documented in trial-report Section (e); follow-up PR optional. (2) **`manual_review_needed=false` misfire** — auto-applies silently, so Phase B blocks Phase D until the rule is fixed by looping back to Phase C IN THIS PR (per Sequencing Phase B). Follow-up PR is reserved for owner-deferred out-of-scope cases (e.g. needing data from a second target project). Rationale for the LOCK: over-fitting to a single target (call-details) defeats the "content-driven, not policy-table" principle (per user 2026-05-19 — different projects need different choices). The trial validates the heuristics; it triggers an in-PR fix when a safety-critical rule misfires, but doesn't accumulate per-target overrides. |
| Restore-manifest v2 backward-compat breaks for users with v1 manifests | `format_version` check in `load_manifest`; v1 manifests dispatch to legacy restore. Test fixture: a v1 manifest from PR #1–#6's apply paths still restores cleanly. |
| `WRITE_NEW` files (`CLAUDE.md.new`) confuse the user — they don't know to merge | Apply-phase final summary print lists every `.new` written + says "merge each `.new` file into the original manually; review with `diff -u CLAUDE.md CLAUDE.md.new`". Same for APPEND_MERGE summary. |
| Tests-passing-for-wrong-reason: the synthetic fixture matches call-details so well that the tests pass via fixture-magic, not contract | Phase A trial is the contract-validation: run the unit tests + the smoke test, THEN run the real trial against `call-details/` — if behaviour diverges, the synthetic fixture was wrong. Phase D documents any divergence. |
| Adoption-mode complexity makes the skill harder for non-developers (the audience) | `--mode=adopt` is opt-in. Greenfield users (the primary audience) never see it. Adoption-mode is for the smaller "existing project owner" audience who already understand "merge conflicts" conceptually. |

## Verification (acceptance criteria)

### Phase 1 — pre-implementation evidence (this plan PR)

1. **Bidirectional plan-review loop**:
   ```bash
   make review-plan-by-codex  PLAN_FILE=docs/plans/2026-05-19-skill-pr7-hybrid-trial-adoption-mode.md ITERATION=N
   make review-plan-by-claude PLAN_FILE=docs/plans/2026-05-19-skill-pr7-hybrid-trial-adoption-mode.md ITERATION=N
   ```
   (BACKLOG f bug fix landed in PR #6, so the make targets work without the manual workaround.)

2. **Pre-next-iter consistency self-check**:
   ```bash
   make review-plan-consistency-by-claude PLAN_FILE=docs/plans/2026-05-19-skill-pr7-hybrid-trial-adoption-mode.md ITERATION=<upcoming>
   ```

3. **Pre-task gate** (per `CLAUDE.md` pre-coding discipline merged in `511a181`): impl PR opening message must state regression safety + outcome measurement.

4. **Mandatory human-approval gate** satisfied.

### Phase 2 — post-implementation gates

**Phase 2 ordering** (closes Codex iter-6 #1 — earlier ordering had `claude[bot] clean` (gate 9) BEFORE `draft impl PR opened` (gate 10), but bots only fire on open PRs): (i) open feat branch + draft impl PR FIRST (gate 1); (ii) implement Phase C (`--mode=adopt` engine + tests); (iii) run Phase 2 gates 2-9 below (local tests + smoke walks + live trial); (iv) flip draft → ready (triggers Tier-2); (v) gate 10 = claude[bot] + Codex Tier-2 clean (closes Codex Tier-2 PR #16 #3 — earlier wording said "gate 9 = claude[bot] clean" but the numbered list correctly has 10 gates including the live trial as gate 9; preamble now matches list, AND Codex GitHub bot IS configured in this project per the live Tier-2 fire on PR #16, so gate 10 now requires BOTH bots clean).

1. Draft impl PR opened from `feat/skill-pr7-hybrid-trial-adoption-mode` branch (closes self-check iter-6.5 #2 + Codex iter-6 #1 + iter-7 #5 — PR opens FIRST per the Phase 2 preamble, so bots can fire on it; previous iters left this gate at position 9-10 in the numbered list despite the preamble fix).
2. `make check` green in skill repo. Impl-PR opening message captures the pre-PR-#7 test-count baseline via `venv/bin/python -m pytest --collect-only -q | tail -1`; gate is "pre-PR-#7 baseline + ~30 new adoption tests, no test deletions" (closes Claude iter-2 #6).
3. `make doctor` green
4. Generated-project smoke walks for all four combinations still pass (python+pip, python+uv, nodejs, go)
5. `--mode=adopt --auto-accept-recommendations` smoke walk against synthetic **all-safe fixture** (only `manual_review_needed=false` collisions; see Bucket D fixture (i)) passes — closes Codex Tier-2 PR #16 #4; the iter-6 #5 fold split Bucket D into two fixtures but the Verification gate kept pointing at the call-details-shaped one which would hang on `manual_review_needed=true` files
6. `--mode=adopt` interactive smoke walk via stdin-injection synthetic input passes
7. `--non-interactive` failure-path test passes
8. Restore-manifest v2 backward-compat with v1 manifest test passes
9. Trial against live `~/Desktop/Code/Boxette/call-details/` — Phase A run committed via `docs/trial-report-pr7.md` Section (a)+(b); Phase D run committed via Section (d)
10. Flip draft → ready; **both** `claude[bot]` AND `chatgpt-codex-connector[bot]` auto-review return clean on the ready-state PR (closes Codex Tier-2 PR #16 #3 — Codex GitHub bot IS configured per the live Tier-2 fire on Plan PR #16; the earlier "NOT configured" claim was a stale CLAUDE.md note. Tracked as a CLAUDE.md correction in BACKLOG PR #7 follow-ups).

## Iteration log (this plan)

| Iter | Findings | Verdict |
|---|---|---|
| 1 (codex) | 7 (4× imp-3, 3× imp-2) | do not implement yet — all 7 folded. Four-questions framework applied; every premise verified empirically. **The reviewer actually ran the dry-run against the live target** (impressive) and caught that my plan's "8 collision files" baseline was off — actual collisions are 4, not 8 (`README.md` + `uv.lock` exist but aren't bootstrap writes; `src/`/`tests/` are dirs not file collisions). Verified locally via `bootstrap.py --dry-run --language python --project-name call-details --out ~/Desktop/Code/Boxette/call-details/` before folding. Folds: P1 CLI contract — `--mode=adopt` is now an adoption MODIFIER requiring `--apply`, NOT a fourth mode in the mutually-exclusive group (resolves the iter-1-draft contradiction); P2 collision baseline corrected to 4 MODIFY + 15 CREATE; P3 default for unknown existing non-empty files = `SKIP` with `manual_review_needed=true` (NOT `WRITE` — was the core `--overwrite-existing` danger under a safer-looking name); P4 Phase D preflight (branch/SHA capture, clean-status check, trial branch creation, restore-manifest round-trip verification); P5 `.new` collision rule (fail-loud if exists) + `APPEND_MERGE` idempotence semantics (line-level only-append-missing); P6 trial-report privacy boundary (no secrets / customer / raw excerpts; safe content = names + sizes + hashes + structural categories); P7 Phase E scoped to docs+BACKLOG only (code polish becomes follow-up PRs). Next: consistency self-check before Claude iter-2 |
| 1.5 (claude consistency self-check, post-iter-1 fold) | 5 drifts | **First run of the post-PR-#6-BACKLOG-(f)-fix `make review-plan-consistency-by-claude`** — and it worked. No "user declined the exit" garbage; actual findings list returned. Caught 5 real drifts I missed during iter-1 fold: (1) Bucket A row still said `--mode={dry-run,diff,apply,adopt}` mutually-exclusive — un-updated when iter-1 #1 was folded; (2) Bucket D smoke-test row still said "8 collisions + 12 clean writes" instead of the empirically-verified 4 + 15; (3) Bucket D adopt-engine row still said "files that should fall through to default WRITE" — the EXACT iter-1 #3 safety bug wording in the test description that fold P3 fixed in the heuristic but not in the test description (caught right where it would have lived as a tests-passing-for-wrong-reason hazard); (4) Bucket E trial-execution commands missing `--project-name` despite fold P1 committing to add it everywhere; (5) Phase A "already done" (Sequencing) vs "future validation" (Bucket E + Risks) contradiction. All 5 folded as in-place wording edits. PR #6's BACKLOG-(f) fix is paying immediate dividends — this self-check would have been useless before. |
| 2 (claude) | 9 (3× imp-3, 4× imp-2, 2× imp-1) | needs another iteration — all 9 folded as (a). The reviewer ran empirical checks against the repo (file structure, render contract, manifest format, BACKLOG state, trial target inventory) and caught 3 real imp-3 blockers: (1) SKILL.md row still framed `--mode` as a 4-value mutually-exclusive group — SAME drift class as iter-1.5 #1, just in Bucket F instead of Bucket A; (2) `--auto-accept-recommendations` × `--non-interactive` semantics contradictory (Scope #1 said mutex, Scope #5 said "fails with exit 2 if non-interactive set" — both can't be true); (3) restore-manifest "v2 bump" missing v1-detection rule — current `Manifest.to_dict()` has NO `format_version` field, so PR #7 is INTRODUCING the field, not bumping it. Plus 4 imp-2: Phase A heuristic-lock ambiguity → committed to LOCK at iter-2 (Phase A is validation-only); docs/usage Modes table needs footnote about `--mode=adopt` being a modifier; test-count target reworded to "pre-PR-#7 baseline + ~30 new"; off-by-one "3 modes" → "4 modes". Plus 2 imp-1: drop PTY mock in favor of stdin-injection for CI portability; add Phase D rollback recipe. All folded as in-place wording edits + Bucket A/B/D/F + Architecture decisions + Risks + Verification. Next: iter-2.5 consistency self-check then iter-3 Codex (convergence re-check) |
| 2.5 (claude consistency self-check, post-iter-2 fold) | 4 drifts | Self-check after iter-2 fold caught 4 leftover wording drifts I missed: (1) Bucket A `_flags.py` row still said `--non-interactive` is "mutually exclusive with `--auto-accept-recommendations`" — un-updated when iter-2 #2 dropped the mutex (same drift class as iter-1.5 #1 reappearing in iter-2; corrected); (2) Bucket D smoke-test policy mapping had `.gitignore` → SKIP and `.python-version` → APPEND_MERGE — APPEND_MERGE is never a valid output for `.python-version` per Scope #5 rule (e). Rule outputs swapped + corrected (`.gitignore` → APPEND_MERGE default; `.python-version` → WRITE/SKIP only); (3) Bucket E Phase A column said "Adjusts heuristic rules if misfire" — contradicts the iter-2 #4 lock decision. Rewrote to validation-only + misfires go to trial-report Section (e); (4) Bucket A help-text was missing the trailing "For read-only inspection, use --diff." sentence that Scope #1 had — implementer couldn't know which was canonical. Aligned. All 4 folded |
| 3 (codex, convergence re-check) | 6 (3× imp-3, 3× imp-2) | needs another iteration — all 6 folded. Note: this run hit a 30-min API stall on first attempt; killed + retried, second attempt completed normally. Findings: **#1 (imp-3) restore rehearsal pointed at the LIVE target, not the copy** — manifests store absolute `target_root` so running `bootstrap.py --restore <live-manifest>` against the live manifest mutates the live target, NOT a post-apply copy. Real bug in Phase D wording. Fixed by reordering: rehearsal-on-COPY happens BEFORE live apply (copy → apply-against-copy → manifest-against-copy → restore-against-copy → verified); live apply runs AFTER rehearsal proves restore works; live manifest is parsed/recorded for rollback-readiness, NOT re-run for verification. **#2 (imp-3) v2 restore would clobber user edits** — `WRITE_NEW`/`APPEND_MERGE` restore semantics bypassed the PR #1 SHA-guard contract (`AGENTS.md:21`: SKIP if current SHA neither before nor after). Fold: v2 entries gain `sha256_after_target_path` field; restore SHA-checks before mutating the actual mutated path (the `.new` file for WRITE_NEW; the original for APPEND_MERGE truncation); user-edit case → SKIP with warning, matching the PR #1 contract. **#3 (imp-3) auto-accept/non-interactive/stdin contradiction** — Scope #6/Bucket C said auto-accept "skips prompts / says yes to all" while Scope #1/#5 said auto-accept only applies `manual_review_needed=false` policies. Tests expected both behaviours. Fold: single-matrix contract — auto-accept handles only `manual_review_needed=false`; `manual_review_needed=true` always needs decision (interactive prompt OR exit-2 under `--non-interactive` / EOF-on-stdin). Bucket C + Scope #6 rewritten. **#4 (imp-2) privacy boundary vs `content_sample`** — `TargetMeta` had `content_sample` field that's exactly the kind of thing that leaks raw target content into reports. Fold: dropped `content_sample`; replaced with derived markers (line_count, heading_count, has_dependency_groups, python_version_pin). Test row asserts user-facing report contains no raw target content. **#5 (imp-2) BACKLOG.md still has the stale 8-collision baseline** at `BACKLOG.md:456` from the PR #6 entry — drift class earlier iters were fixing. Bucket F BACKLOG row updated to mark entries (c)(d)(e) ✅ Done + correct count to verified 4 + 15. **#6 (imp-2) `--diff` policy annotation underspecified** — `--mode=adopt` invalid with `--diff` but Scope #8 said `--diff` gets adoption annotations. Implementer can't know whether to skip annotations or add invalid `--diff --mode=adopt`. Fold: plain `--diff --language python` runs the analyzer in read-only mode (no manifest, no writes, no `--mode=adopt` needed) and annotates diff headers with policy recommendations; non-Python `--diff` keeps the pre-PR-#7 behaviour. Test added. **Trajectory imp-3**: PR #6 hit 3 → 0 → 1 → 0 → 1 → 1 plateau pattern; PR #7 is at 3 → 3 in 3 iters but findings ARE narrowing in scope (iter-1 was conceptual gaps; iter-3 is specific contract leaks like missing SHA-guard fields). Next: consistency self-check, then iter-4 Codex to confirm convergence |
| 3.5 (claude consistency self-check, post-iter-3 fold) | 4 drifts | Self-check after iter-3 fold caught 4 cross-section propagation drifts I missed (same drift class as iter-1.5 #1 + iter-2.5 #1 — iter-N fold updates one section but not all): (1) Sequencing Phase D still had old "verify restore round-trip on copy AFTER apply" wording — Bucket E got updated to rehearsal-on-copy-BEFORE-live-apply but Sequencing didn't; (2) Sequencing Phase B said "draft the recommendation heuristics" — contradicts iter-2 #4 heuristic-lock; (3) Scope #11 trial-report Section (c) description "what the trial changed" — contradicts heuristic-lock; (4) Scope #11 Section (d) vs Bucket E Section (d) content drift — Bucket E added "UX gaps?" (belongs in (e)) + "rehearsal result" (belongs in (d) but not in Scope #11's canonical definition). All 4 folded as in-place wording edits. **Pattern observation**: iter-1.5, iter-2.5, iter-3.5 all caught the SAME drift class (cross-section propagation). The four-questions Q3 check is supposed to catch this during fold but I keep missing it. The self-check tool keeps saving the loop. Next: iter-4 Codex |
| 4 (codex, convergence re-check) | 6 (5× imp-3, 1× imp-2) | needs another iteration — all 6 folded. **Trajectory not converging**: Codex imp-3 went 3 → 3 → 3 → 5. Each iter Codex goes DEEPER and surfaces NEW classes (this iter: privacy + disk catastrophe from `cp -a` of 14GB+ live target; wrong-branch rollback hardcoded; rehearsal-hang on manual-review prompts; missing v2 field; impossible Codex auto-review gate; stale collision-contract text). NOT the PR #6 narrowing pattern. All real per CLAUDE.md imp-3 calibration: PR doesn't work without each fix. First attempt at iter-4 stalled 15 min (API issue, like iter-3 first attempt); killed + retried; second attempt completed normally. **#1 (imp-3, real safety)** `cp -a <target>` would copy 14GB `data/` + 1.3GB `.venv/` + `secrets/` to `/tmp/` — privacy + disk catastrophe. Fix: minimal-copy via `mktemp -d` with ONLY the 4 collision files + parent dirs; `trap` cleanup. **#2 (imp-3)** rollback hardcoded `checkout main` but live target is on `customer-cases-redial-linking-v1`. Fix: capture `ORIGINAL_BRANCH` + `ORIGINAL_SHA` at preflight; rollback uses captured branch. **#3 (imp-3)** rehearsal apply would hang/exit-2 on `manual_review_needed=true` collisions (CLAUDE.md + pyproject.toml). Fix: pre-recorded rehearsal-decision-script in trial-report Section (b); piped via heredoc; live apply MUST use same decisions or restart rehearsal. **#4 (imp-3)** Bucket B restore semantics referenced `sha256_before_target_path` but didn't list it in v2 fields. Fix: explicit per-policy field definitions (WRITE_NEW: target=`<path>.new`, sha256_before=None; APPEND_MERGE: target=`<path>`, sha256_before=pre-append sha; SKIP: all None) + missing-file handling for WRITE_NEW restore. **#5 (imp-3)** Verification gate #9 required "Codex auto-review clean" but repo's `github_review='claude'` — Codex GitHub bot NOT configured per CLAUDE.md. Fix: gate now requires claude[bot] clean only; Codex extension is for future PRs that enable `--github-review=both-docs`. **#6 (imp-2)** SKILL.md + docs/usage.md still have stale collision-contract wording. Fix: edit existing safety paragraphs to distinguish plain `--apply` (aborts) vs `--apply --mode=adopt` (per-file). **Decision point per CLAUDE.md plateau rule**: 4 Codex iters in; imp-3 count 3/3/3/5; findings ARE real but Codex keeps finding new classes (not narrowing). Surface to user for the convergence decision before iter-5 |
| 4.5 (claude consistency self-check, post-iter-4 fold) | 6 drifts | Sixth consecutive iter-N.5 self-check catching cross-section propagation drifts (after iter-1.5, 2.5, 3.5). User chose "Run iter-5 Codex (full convergence)" at the iter-4 plateau decision point. Self-check caught 6 drifts: (1) **CRITICAL — Bucket E STILL had `cp -a` of 14GB+ target** despite iter-4 #1 fix in Sequencing — would have caused the exact catastrophe iter-4 #1 was supposed to prevent. Fixed: Bucket E now references Sequencing's canonical recipe instead of inlining a stale copy; (2) Section (b) overloaded — Phase A findings vs rehearsal decisions. Fixed: Section (b) definition extended to include rehearsal-decision-script; (3) Bucket E Section (d) still listed "UX gaps?" (belongs in (e)); (4) Bucket E missing rehearsal-decision-script mechanism (heredoc input); (5) Iter-4 evidence rows missing from Evidence table (6 folds described in iter log row but not enumerated below); (6) Risks row only covered partial trigger for non-TTY exit-2 path. All 6 folded. **Pattern observation #2**: iter-4 fold had MORE cross-section drift than any prior iter; same drift class. Self-check has now caught 23 cross-section drifts across the loop. The four-questions Q3 check during fold catches none of them despite my conscious effort. Self-check is doing the work the manual review can't. |
| 5 (codex, full convergence per user choice at iter-4 plateau gate) | 5 (3× imp-3, 1× imp-2, 1× imp-1) | needs another iteration — all 5 folded. **Trajectory: 3/3/3/5/3 across 5 Codex iters — clear plateau pattern at 3-5 imp-3, not converging to 0**. First attempt at iter-5 stalled 23 min (third consecutive Codex API stall — iter-3 was 30 min, iter-4 was 15 min). Retried; second attempt completed normally. iter-5 folds: **#1 (imp-3)** non-TTY exit-2 logic contradicts heredoc rehearsal — heredoc input is non-TTY but should succeed. Fix: EOF is the failure condition, not non-TTY. **#2 (imp-3)** apply/manifest mapping underspec'd — existing `_apply_writes` doesn't handle SKIP/WRITE_NEW/APPEND_MERGE. Fix: new `plan_adoption_entries` + `_apply_adoption_writes` functions with per-policy write contract. **#3 (imp-3)** interactive prompt offered `[o]verwrite` + `[a]ppend` for every manual-review file — would let user APPEND_MERGE pyproject.toml or OVERWRITE CLAUDE.md (corruption risk). Fix: per-file allowed-actions matrix; APPEND_MERGE only for `.gitignore`; OVERWRITE requires typed `OVERWRITE` (uppercase) confirmation. **#4 (imp-2)** Phase A both pre-impl + post-impl. Fix: post-impl only. **#5 (imp-1)** CLAUDE.md line-count stale (97 → 96). **Surface to user (second plateau gate)** — 5 Codex iters in, imp-3 not converging; each iter still surfaces real safety/correctness gaps but the cost-per-imp-3 is rising (API stalls + my fold-then-self-check-catches-drift pattern). |
| 5.5 (claude consistency self-check, post-iter-5 fold, user-requested separation from commit/launch) | 5 drifts | Seventh consecutive iter-N.5 self-check catching cross-section propagation drifts (after iter-1.5/2.5/3.5/4.5). Caught: (1) Sequencing item 1 still said "Phase A no new code yet" (pre-impl wording) despite iter-5 #4 making Phase A post-impl-only — same drift class repeating; (2) Scope #8 diff-annotation example still said `97 lines` despite iter-5 #5 fold; (3) Bucket A `cli.py` row referenced `_apply_writes` not the new `_apply_adoption_writes` per iter-5 #2 fold; (4) iter-5 evidence rows missing from Evidence table; (5) Sequencing A→B→C ordering temporally inconsistent with iter-5 #4 (A is post-impl; B validates A so B must also be post-impl; can't be before C). All 5 folded. **Self-check has now caught 28 cross-section drifts total across the loop**. User chose "run iter-6 separately" — next step is launching iter-6 as its own separate command. |
| 6 (codex, third plateau gate user-requested continue) | 6 (6× imp-3) | needs another iteration — all 6 folded. **Trajectory: 3/3/3/5/3/6 — went UP across the loop**. Driver-recommendation at third gate was "ship now"; user chose "continue iter-7 Codex". Folds: **#1 (imp-3)** Phase 2 gates ordering impossible — claude[bot] (gate 9) listed BEFORE "draft PR opened" (gate 10), but bots only fire on open PRs. Fix: added explicit Phase 2 ordering preamble (open draft PR first → impl → gates 1-8 → ready → gate 9 = claude[bot] clean). **#2 (imp-3)** Bucket A row mentioned both `_apply_writes` and `_apply_adoption_writes` parseable two ways. Fix: rewrote to ONLY reference `_apply_adoption_writes` for adopt-mode (legacy `_apply_writes` stays for plain `--apply` only). **#3 (imp-3)** v2 manifest contract self-contradicted on SKIP (no manifest entry vs has manifest fields). Fix: SKIP entries NOT in restore manifest (mutation-only); SKIP decisions live in adoption report + trial-report Section (b). **#4 (imp-3, REAL EMPIRICAL)** AGENTS.md is gitignored in `call-details/.gitignore:48` (verified `git check-ignore -v` returns `.gitignore:48:AGENTS.md`). Vanilla CREATE would land on disk but never appear in `git diff --stat` — misleading trial result. Fix: Phase A preflight runs `git check-ignore -v --` against every planned-CREATE; owner decision per match (skip / .new / un-ignore). **#5 (imp-3)** auto-accept smoke test would hang on call-details-shaped fixture (CLAUDE.md + pyproject.toml are manual_review_needed=true). Fix: split into 2 fixtures — all-safe + call-details-shaped with stdin decision script. **#6 (imp-3)** rollback recipe details in evidence rows but not in active Phase D text. Fix: Bucket E Phase D now has executable 4-step recipe inline (preflight + rehearsal + apply + rollback) with exact commands; diff scoped to planned files only. **Self-check + iter-7 still to come per user direction.** |
| 6.5 (claude consistency self-check, post-iter-6 fold, separated per user direction) | 5 drifts | 8th consecutive iter-N.5 catching cross-section propagation drifts (33 cumulative). All real, all (a)-folded: (1) Bucket A row STILL had a dangling `_apply_writes per the agreed policies` sentence from pre-iter-5 wording; same drift class as iter-5.5 #3 reappearing. (2) Phase 2 numbered list still had draft-PR-opened as gate 10 despite iter-6 #1 preamble; re-numbered. (3) iter-6 evidence rows missing from Evidence table; added (×6) consolidation row + (×5) iter-6.5 row. (4) `git check-ignore` preflight attributed to both Phase A AND Phase D; resolved as Phase D preflight. (5) Trial-report Section (a) overloaded; per Scope #11 it's pure inventory; decision content goes in (b). Loop discipline holding: every fold cycle introduces ~5 new cross-section drifts that the manual review misses. Next: iter-7 Codex (separate command). |
| 7 (codex, final iter — user accepted driver "ship now" recommendation at fourth plateau gate) | 7 (4× imp-3, 3× imp-2) | LAST loop iteration. Trajectory across 7 Codex iters: 3/3/3/5/3/6/4 imp-3 — no narrowing trend. Same-AI 0-imp-3 rule served PR #6 well at 6 iters; PR #7's wider scope (hybrid trial + adoption-mode redesign + new CLI contract + manifest v2 + interactive UX + live-target operations) does not converge under the same rule. **Decision (user 2026-05-19 at fourth plateau gate, driver-recommended)**: ship Plan PR #7 now; next-class findings get caught by impl PR Tier-1 review with the same rigor. All 7 iter-7 findings folded. **Trajectory: 3/3/3/5/3/6/4 imp-3 across 7 Codex iters — clear plateau, not converging**. Folds: **#1 (imp-3)** ignored planned-creates still had no CLI enforcement path. Fix: `TargetMeta.ignored_by_git` field; recommend_policy uses it. **#2 (imp-3)** v2 restore semantics only covered WRITE_NEW + APPEND_MERGE; WRITE + OVERWRITE undefined. Fix: full per-policy restore matrix (a)-(e). **#3 (imp-3)** `plan_adoption_entries(target_root, adoption_plan)` couldn't compute after-SHAs without planned_files. Fix: signature includes `planned_files`. **#4 (imp-3)** Phase B "documented + applied" was unsafe for `manual_review_needed=false` misfires. Fix: Phase B is now a hard gate for live Phase D apply when misfires affect non-prompted policies. **#5 (imp-2)** Phase 2 ordering AGAIN — third consecutive time the gate-list didn't match the preamble. Fix: numbered list re-ordered with draft-PR-opened as gate 1. **#6 (imp-2)** `--mode=adopt` Python-only declared but not validated. Fix: CLI rejects `--mode=adopt --language=nodejs/go`. **#7 (imp-2)** `--overwrite-existing` × `--mode=adopt` interaction undefined. Fix: CLIError on the combination. |

## Evidence table — what was folded and where

| Iter | Importance | Finding | Action |
|---|---|---|---|
| 1 | 3 | **(a) fold** — Codex iter-1 #1: CLI contract contradictory — plan said `--mode=adopt` joined the mutually-exclusive `--dry-run`/`--diff`/`--apply` group AND required explicit `--apply --mode=adopt`. Cannot both be true. Plus trial commands missing `--project-name` (current `_resolve_mode` requires it). Implementer couldn't know intended argparse contract | Resolved: `--mode=adopt` is an adoption modifier that REQUIRES `--apply` (write safety — never auto-implies an apply); is invalid with `--dry-run`/`--diff`/`--restore`. NOT a fourth element of mode-group. Updated Pre-loop scope, Scope #1, Bucket A row, Sequencing, Verification commands (added `--project-name call-details` to every trial invocation). |
| 1 | 3 | **(a) fold** — Codex iter-1 #2: "8 collision files" baseline was false. Codex actually ran the dry-run + file-existence check against the live target and found 4 actual collisions (`CLAUDE.md`, `pyproject.toml`, `.gitignore`, `.python-version`); `README.md` + `uv.lock` exist in target but skill never writes them; `src/`/`tests/` are dirs (bootstrap writes files inside, not the dirs) | Re-ran dry-run locally during fold: confirmed 4 MODIFY + 15 CREATE. Updated Context "Empirical collision baseline" subsection with verbatim file lists. Updated heuristic rules in Scope #5 to match real collision set (4 files with specific heuristics: .gitignore APPEND_MERGE, .python-version WRITE-if-version-matches-else-SKIP, CLAUDE.md WRITE_NEW, pyproject.toml SKIP). Dropped misfiring rule (h) "src/ or tests/ directories populated → SKIP" since the bootstrap doesn't write directory entries. |
| 1 | 3 | **(a) fold — CRITICAL SAFETY** — Codex iter-1 #3: tests row said "files that should fall through to default WRITE" — for adoption into existing projects, an existing non-empty unrecognized file defaulting to WRITE recreates the `--overwrite-existing` danger under a safer-looking mode, especially with `--auto-accept-recommendations` | Default for unknown existing non-empty file is now `SKIP` with `manual_review_needed=true` (NOT `WRITE`). Only `WRITE` for: missing target, empty target, or byte-identical target. `--auto-accept-recommendations` REFUSES policies with `manual_review_needed=true` — falls back to interactive prompt OR fails with exit 2 if `--non-interactive` also set. This is the core safety guarantee that distinguishes `--mode=adopt` from `--overwrite-existing`. |
| 1 | 3 | **(a) fold** — Codex iter-1 #4: live apply against `call-details/` has no clean-state preflight. Target's `git status --short` is already dirty (modified `.claude/settings.json`, untracked `.claude/scheduled_tasks.lock`, untracked `tools/`). Restore manifest only protects bootstrap-written paths — no clean experiment boundary or audit trail for the live repo | Phase D preflight added: capture branch/SHA, require clean `git status --short` OR explicit owner waiver naming the dirty paths, create a fresh `feat/pr7-trial-adoption` branch in the TARGET (not the skill repo), record planned-files list; run apply; record `git diff --stat`; verify restore-manifest round-trip on a copy of the target before declaring success. Sequencing section updated. |
| 1 | 2 | **(a) fold** — Codex iter-1 #5: `.new` and `APPEND_MERGE` policies lacked collision/idempotence rules. `WRITE_NEW` would clobber a stale `<path>.new`; `APPEND_MERGE` "contains skill's patterns" wording was ambiguous (re-append vs only-missing) | `.new` collision rule: fail-loud with clear message if `<original>.new` already exists (alternative rotation-suffix parked as future BACKLOG). `APPEND_MERGE` semantics: line-level set-comparison after stripping; append only missing lines; preserves order of new additions; running twice produces same result. Restore semantics extended to truncate-to-captured-pre-append-length. Scope #7 + Bucket B updated. |
| 1 | 2 | **(a) fold** — Codex iter-1 #6: trial-report privacy boundary missing. Live target contains `secrets/`, `data/`, customer-domain operational docs; future skill repo is public-bound | Privacy-boundary subsection added to trial-report row (Scope #11): NO secrets / customer / PII data / raw file excerpts. Safe content = filenames + sizes + sha256 hashes + structural categories + policy decisions. Default policy: summarize without quoting. |
| 1 | 2 | **(a) fold** — Codex iter-1 #7: Phase E was open-ended scope creep — "Any small fixes / docs improvements / BACKLOG entries". PR already changes CLI grammar + manifest format + interactive UX + restore behaviour + tests + docs + live-repo trial | Phase E narrowed: docs additions + BACKLOG entries only. Code polish that the trial surfaces becomes a follow-up PR unless it blocks the adoption-mode acceptance gates (in which case it folds into Phase B/C before that gate). Sequencing wording updated. |
| 2 | 3 | **(a) fold** — Claude iter-2 #1: SKILL.md invocation block still framed `--mode` as `[--mode {dry-run,diff,apply,adopt}]` (4-value mutex) despite iter-1 #1 + iter-1.5 #1 folds — same drift class as iter-1.5, just in Bucket F instead of Bucket A. User reading SKILL.md would try `--mode=dry-run` and get an argparse error | Bucket F SKILL.md row updated to `[--mode adopt]` single-value modifier semantics matching Bucket A. Added explicit "requires `--apply`; invalid with `--dry-run`/`--diff`/`--restore`" note. |
| 2 | 3 | **(a) fold** — Claude iter-2 #2: `--auto-accept-recommendations` × `--non-interactive` semantics contradictory across scope rows (Scope #1 said mutually exclusive; Scope #5 said "fails with exit 2 if non-interactive also set" — both can't be true). Unit test "fallback fails with exit 2" unreachable under mutex; tests-passing-for-wrong-reason hazard | Dropped the mutex per reviewer's recommended option (a) — the more useful CI contract. New combined semantics: `--auto-accept` accepts every `manual_review_needed=false` policy without prompt; `manual_review_needed=true` policies still need prompt; under `--non-interactive` (combinable), those prompts become exit-2 failures (the natural CI contract: "accept everything safe, fail on anything that needs human review"). Bucket A flag-validation updated. Bucket D test row updated with the CI-safety matrix (4 combinations explicitly tested). Scope #5 rewritten. |
| 2 | 3 | **(a) fold** — Claude iter-2 #3: restore-manifest "v2 bump" missing v1-detection rule. Current `manifest.py` Manifest.to_dict() has NO `format_version` field — so PR #7 is INTRODUCING the field, not bumping it. An existing v1 manifest in `$TMPDIR` would have the field missing, and an `if m.format_version == 2:` check would be False, with no documented `else` path | Architecture decisions bullet rewritten: introduces `format_version: 2`; `Manifest.from_dict` treats missing/absent `format_version` as v1 (PR #1–#6 shape) and `format_version == 2` as v2. v1 manifests dispatch to legacy restore path. Bucket B `manifest.py` row updated with explicit dispatch logic + added `pre_append_length` entry-level field for APPEND_MERGE truncation rollback. Field name `format_version` (snake_case) committed since it lands in JSON forever. |
| 2 | 2 | **(a) fold** — Claude iter-2 #4: Phase A vs Bucket B sequencing ambiguity — Scope #5 fully specified heuristics; Risks said "hold off Bucket B lock until Phase A data is in". Implementer can't know whether heuristics are locked or draft | Resolved per reviewer's recommended option (b): heuristics LOCKED at iter-2. Phase A is validation-only; misfires go to `docs/trial-report-pr7.md` Section (e) as UX gaps; remediation is a follow-up PR. Rationale: over-fitting to a single target (call-details) defeats the "content-driven, not policy-table" principle. Risks row rewritten. |
| 2 | 2 | **(a) fold** — Claude iter-2 #5: `docs/usage.md` Modes table (4 modes: dry-run/diff/apply/restore) and the new Adoption-mode section would coexist without explanation — reader sees 4 modes in the table, then a section that uses `--mode=adopt` which the table doesn't describe | Bucket F `docs/usage.md` row gains explicit Modes-table edit: add footnote/note under the existing table — "`--mode=adopt` is a per-file adoption modifier of `--apply` — NOT a 5th mode. See the Adoption mode section below." Defeats the iter-1 #1 fold otherwise. |
| 2 | 2 | **(a) fold** — Claude iter-2 #6: Verification gate #1's "existing 351 tests + ~30 new" used a frozen number that may drift between plan and impl (unrelated test additions). Either wrong-now hazard OR brittleness hazard | Reworded to "pre-PR-#7 baseline + ~30 new adoption tests, no test deletions". Impl-PR opening message captures the baseline via `pytest --collect-only -q | tail -1`. No frozen number. |
| 2 | 2 | **(a) fold** — Claude iter-2 #7: Bucket A `cli.py` row said `_resolve_mode` "unchanged for the existing 3 modes (dry-run / diff / apply / restore)" — 4 modes listed in parenthetical, off-by-one count | Reworded to "4 modes". |
| 2 | 1 | **(a) fold** — Claude iter-2 #8: Scope #9 tests row said "PTY mock OR `--auto-accept-recommendations`" — gave implementer the option to pick the flaky path (PTY mocks vary across CI runners, especially macOS) | Dropped PTY mock from in-scope. Committed to stdin-injection synthetic input for manual-review prompt cases that auto-accept can't cover. Parked PTY-mock-based richer interactive testing in BACKLOG. Verification gate #5 reworded. |
| 2 | 1 | **(a) fold** — Claude iter-2 #9: Phase D live-apply lacked a documented "if it goes wrong" recipe. Non-developer audience needs explicit steps, not just "use `--restore`" | Phase D row gains a Rollback recipe sub-block: 5-step recipe (run `bootstrap.py --restore`, `git status` verify, `rm` residual `.new` files, check `git diff` for APPEND_MERGE residue, branch cleanup). |
| 3 | 3 | **(a) fold** — Codex iter-3 #1: Phase D's "verify restore on a copy of the target AFTER live apply" is not executable as written — manifests store absolute `target_root`, so running restore against a post-apply manifest would mutate the LIVE target, not a post-apply copy. Real safety bug | Reordered: rehearsal-on-COPY happens BEFORE live apply (`cp -a` target → apply-against-copy → restore-against-copy verifies semantics). Live apply runs AFTER rehearsal. Live manifest is parsed/recorded for rollback-readiness, NOT re-run for verification. |
| 3 | 3 | **(a) fold** — Codex iter-3 #2: v2 restore semantics (`WRITE_NEW` removes `.new`; `APPEND_MERGE` truncates) bypassed the PR #1 SHA-guard contract (`AGENTS.md:21`: SKIP if current SHA neither before nor after). User edits to `.gitignore` or `CLAUDE.md.new` between apply and restore could be deleted/truncated | Added `sha256_after_target_path` field to v2 manifest entries. Restore SHA-checks before mutating the actual mutated path: current_sha == sha256_after → mutate; matches sha256_before → no-op; else → SKIP with warning. Path-safety validation on `target_path` added. |
| 3 | 3 | **(a) fold** — Codex iter-3 #3: auto-accept / non-interactive / stdin behaviour contradictory across Scope #6 + Bucket C ("skips prompts / says yes to all") vs Scope #1/#5 ("only applies `manual_review_needed=false`"). Tests expected both — implementer could auto-apply manual-review policies OR create CI hangs | Single-matrix contract: auto-accept handles ONLY `manual_review_needed=false`; `manual_review_needed=true` always needs a decision (interactive prompt; OR exit-2 under `--non-interactive` / EOF-on-stdin). Bucket C + Scope #6 rewritten. |
| 3 | 2 | **(a) fold** — Codex iter-3 #4: `TargetMeta.content_sample` field contradicts Scope #11 privacy boundary — a "sample" field leaks raw target content into reports/logs | Dropped `content_sample`. Replaced with derived markers: `line_count`, `heading_count` (markdown), `has_dependency_groups` (pyproject), `python_version_pin` (.python-version). Test row asserts no raw target content in user-facing report. |
| 3 | 2 | **(a) fold** — Codex iter-3 #5: `BACKLOG.md:456` (the PR #6 PR #7 entry) still has stale "8 collisions, ~12 cleanly writable" baseline; plan now says 4 MODIFY + 15 CREATE — exactly the drift class earlier iters were fixing | Bucket F BACKLOG row gains: mark PR #7 BACKLOG entries (c)(d)(e) ✅ Done + correct collision count to verified 4 + 15. |
| 3 | 2 | **(a) fold** — Codex iter-3 #6: `--mode=adopt` invalid with `--diff` but Scope #8 said `--diff` gets adoption-mode policy annotations. Implementer can't know whether to skip annotations or accidentally allow `--diff --mode=adopt` | Plain `--diff --language python` runs analyzer in read-only mode (no manifest, no writes); annotates diff headers with policy recommendations; no `--mode=adopt` needed. Non-Python `--diff` keeps PR #1 behaviour. Test added. |
| 4 | 3 | **(a) fold — REAL SAFETY (privacy + disk)** — Codex iter-4 #1: `cp -a <target>` would copy 14GB `data/` + 1.3GB `.venv/` + `secrets/` from call-details to `/tmp/` | Minimal-copy via `mktemp -d`: only 4 collision files + parent dirs + `trap` cleanup. No customer data leaves the live target. |
| 4 | 3 | **(a) fold** — Codex iter-4 #2: rollback hardcoded `checkout main` but live target is on `customer-cases-redial-linking-v1` | Capture `ORIGINAL_BRANCH` + `ORIGINAL_SHA` at preflight; rollback uses captured branch. Trial branch created from `ORIGINAL_SHA`. |
| 4 | 3 | **(a) fold** — Codex iter-4 #3: rehearsal apply would hang/exit-2 on `manual_review_needed=true` collisions (CLAUDE.md + pyproject.toml) with bare stdin | Pre-recorded rehearsal decisions in trial-report Section (b); piped via heredoc. Live apply uses same decisions or restarts rehearsal. |
| 4 | 3 | **(a) fold** — Codex iter-4 #4: Bucket B referenced `sha256_before_target_path` in restore semantics but didn't list it in v2 fields | Explicit per-policy field definitions added: WRITE_NEW (`target_path=<path>.new`, before=None); APPEND_MERGE (`target_path=<path>`, before=pre-append sha, `pre_append_length`); SKIP (all None). Missing-file handling for WRITE_NEW restore specified. |
| 4 | 3 | **(a) fold** — Codex iter-4 #5: Verification gate "Codex auto-review return clean" — but repo's `github_review='claude'`, Codex GitHub bot NOT configured per CLAUDE.md | Gate reworded: claude[bot] clean only; Codex extension is for future PRs that enable `--github-review=both-docs`. |
| 4 | 2 | **(a) fold** — Codex iter-4 #6: SKILL.md + docs/usage.md collision-contract text would coexist with new adoption-mode section without acknowledging the new escape hatch | Existing safety-contract paragraphs edited to distinguish plain `--apply` (aborts) vs `--apply --mode=adopt` (per-file recommendations). |
| 4.5 | (×6) | **(a) fold all 6 self-check drifts** — see iter log row 4.5 for details. **Critical fix #1**: Bucket E still had stale `cp -a` despite iter-4 #1 fix in Sequencing (would have re-introduced the 14GB+ catastrophe Codex iter-4 #1 was supposed to prevent). Bucket E now references Sequencing's canonical recipe. Other folds: (2) Section (b) definition extended; (3) Bucket E (d) UX-gaps removed; (4) Bucket E heredoc decision-script mechanism added; (5) iter-4 evidence rows added (this consolidation); (6) Risks row extended with second exit-2 trigger | All 6 folded as in-place edits. |
| 5 | 3 | **(a) fold** — Codex iter-5 #1: non-TTY exit-2 contradicts heredoc rehearsal (heredoc input is non-TTY but should succeed) | Risks row updated: EOF is the failure condition, NOT non-TTY by itself; heredoc/pipe input works; `--non-interactive` forces exit-2 for manual-review prompts regardless of TTY. |
| 5 | 3 | **(a) fold** — Codex iter-5 #2: apply/manifest mapping underspec'd — existing `_apply_writes` doesn't handle SKIP/WRITE_NEW/APPEND_MERGE | New `plan_adoption_entries(target_root, adoption_plan) → entries` (manifest.py) + `_apply_adoption_writes(root, planned_files, adoption_plan, entries)` (cli.py) functions. Per-policy write contract spec'd: SKIP (no write), WRITE (root/rel_path), OVERWRITE (root/rel_path + before-snapshot), WRITE_NEW (root/<rel_path>.new), APPEND_MERGE (atomic append-only-missing). |
| 5 | 3 | **(a) fold** — Codex iter-5 #3: interactive prompt offered `[o]verwrite` + `[a]ppend` for every manual-review file → user could APPEND_MERGE pyproject.toml or OVERWRITE CLAUDE.md (corruption risk) | Per-file allowed-actions matrix added to Scope #6: APPEND_MERGE available only for `.gitignore`; OVERWRITE requires typed `OVERWRITE` (uppercase) confirmation; unavailable actions not shown in the per-file prompt. |
| 5 | 2 | **(a) fold** — Codex iter-5 #4: Phase A both pre-impl prerequisite (Sequencing) and post-impl gate (Verification) | Phase A is now post-impl only (Sequencing + Bucket E + Verification aligned). Sequencing temporal-ordering preamble added: C precedes A+B in time. |
| 5 | 1 | **(a) fold** — Codex iter-5 #5: CLAUDE.md line-count 97 stale (current is 96) | Context bumped to ~96; Scope #8 diff-annotation example updated; line counts framed as runtime values, not plan-frozen. |
| 5.5 | (×5) | **(a) fold all 5 self-check drifts** — see iter log row 5.5 (next iter row). Critical: Sequencing item 1 still said "Phase A no new code yet" (pre-impl wording) despite iter-5 #4 making Phase A post-impl-only — same drift class as iter-1.5/2.5/3.5/4.5 #1. Other folds: Scope #8 line-count example update; Bucket A cli.py row referenced wrong function name (`_apply_writes` not `_apply_adoption_writes`); 5 iter-5 evidence rows added (this consolidation); Sequencing logical A→B→C ordering reconciled with the post-impl-A rule (C precedes A+B in time). | All 5 folded. |
| 6 | (×6) | **(a) fold all 6 imp-3** — see iter log row 6. Folds: (1) Phase 2 gate-list ordering wrong (claude[bot] before draft-PR-opened); fixed with explicit ordering preamble. (2) Bucket A double-mentioned `_apply_writes` AND `_apply_adoption_writes`; rewrote to ONLY reference adopt path. (3) v2 manifest contract self-contradicted on SKIP; SKIP entries now NOT in restore manifest. (4) AGENTS.md gitignored in target (verified empirically); Phase D preflight runs `git check-ignore -v --` per planned-CREATE. (5) auto-accept smoke would hang on manual-review fixture; split into all-safe + call-details-shaped (stdin heredoc). (6) rollback recipe only in evidence; Bucket E now has executable 4-step inline recipe. | All 6 folded. |
| 6.5 | (×5) | **(a) fold all 5 self-check drifts** — see iter log row 6.5 (next). (1) Bucket A row STILL had dangling `_apply_writes per the agreed policies` sentence from pre-iter-5 wording; removed; (2) Phase 2 numbered list still had draft-PR-opened as gate 10 (last) despite iter-6 #1 fold's preamble — re-ordered list to put draft-PR-opened FIRST; (3) iter-6 evidence rows missing — this consolidation row + (×5) iter-6.5 row added; (4) `git check-ignore` preflight attributed to both Phase A AND Phase D; resolved as Phase D preflight (Phase A is empirical-findings phase, not execution); (5) trial-report Section (a) overloaded with decision content; resolved per Scope #11 (Section (a) = pure inventory; decisions go in Section (b)). | All 5 folded. |
| 7 | 3 | **(a) fold** — Codex iter-7 #1: ignored planned-creates documented but no CLI enforcement path. AGENTS.md case would silently land on disk + never commit | `TargetMeta` gains `ignored_by_git: str \| None` field (closes iter-7 #1). recommend_policy uses it: ignored CREATE → recommend SKIP (default) OR WRITE_NEW with `manual_review_needed=true`. |
| 7 | 3 | **(a) fold** — Codex iter-7 #2: v2 restore semantics covered only WRITE_NEW + APPEND_MERGE in detail; WRITE + OVERWRITE undefined | Full per-policy v2 restore matrix (a)-(e) added to Bucket B: WRITE (remove created file), OVERWRITE (write content_before_b64 back + restore mode), WRITE_NEW (remove .new), APPEND_MERGE (truncate to pre_append_length), SKIP (no manifest entry, nothing to restore). All SHA-guarded. |
| 7 | 3 | **(a) fold** — Codex iter-7 #3: `plan_adoption_entries(target_root, adoption_plan)` couldn't compute after-SHAs without access to rendered bytes; TargetMeta deliberately excludes raw content | Signature changed to `plan_adoption_entries(target_root, planned_files, adoption_plan)`. Tests assert after-SHA values for each mutating policy. |
| 7 | 3 | **(a) fold** — Codex iter-7 #4: Phase B "documented and applied" was unsafe for `manual_review_needed=false` misfires (those auto-apply silently) | Phase B is now a hard gate for live Phase D: misfires on non-prompted policies stop Phase D until either owner overrides via interactive prompt during rehearsal OR Phase C is amended in follow-up PR. Doc-only "park to follow-up" only acceptable for prompted policies. |
| 7 | 2 | **(a) fold** — Codex iter-7 #5: Phase 2 ordering preamble said draft-PR-opened FIRST but numbered list still had it at position 9 (third consecutive iter with this propagation failure) | Numbered list re-ordered: gate 1 = draft PR opened; gates 2-9 are the impl + tests + smoke walks + trial; gate 10 = flip-to-ready + claude[bot] clean. |
| 7 | 2 | **(a) fold** — Codex iter-7 #6: `--mode=adopt` Python-only declared in NOT-in-scope but not CLI-validated | Bucket A validation: `--mode=adopt --language=nodejs/go` → CLIError. Tests added. |
| 7 | 2 | **(a) fold** — Codex iter-7 #7: `--overwrite-existing` × `--mode=adopt` interaction undefined (two conflicting consent paths) | Bucket A validation: `--mode=adopt --overwrite-existing` → CLIError (adopt-mode's per-file consent IS the consent model). Tests added. |
| Tier-2 | 1 | **(a) fold** — claude[bot] Tier-2 #3: iter-7 evidence rows aggregated, not individual per finding (precedent: iter-1-6 have detailed breakdowns) | Added the 7 individual iter-7 rows above (this fold's product). |
| Tier-2 | 3 | **(a) fold** — Codex Tier-2 PR #16 #1 (P1): Phase B override path for `manual_review_needed=false` misfires referenced "the interactive prompt during rehearsal," but Scope #6 + Bucket C only prompt `manual_review_needed=true` files; primary mitigation path was impossible to execute. My iter-7 #4 fold introduced this contradiction; consistency self-check didn't catch it because the per-section check didn't cross-walk Phase B's override mechanism against Scope #6's prompt eligibility. | Sequencing Phase B: removed the impossible "interactive prompt override" path; `manual_review_needed=false` misfires now block Phase D until Phase C is amended in follow-up PR (the only mitigation path that the single-matrix design actually permits). |
| Tier-2 | 2 | **(a) fold** — Codex Tier-2 PR #16 #2 (P2): Bucket B `target_path` field-definition clause said "for SKIP / OVERWRITE / WRITE: same as `path`" while the per-policy matrix said "(e) SKIP policy → NOT in manifest at all"; the iter-6 #3 mutation-only-manifest fold updated the matrix but left the field-definition stale | Bucket B field-definition clause: SKIP removed from the `target_path` list; explicit note that SKIP has no manifest entry so no `target_path` value applies. |
| Tier-2 | 2 | **(a) fold** — Codex Tier-2 PR #16 #3 (P2): Phase 2 preamble said "gate 9 = claude[bot] clean" but the numbered list has 10 gates with claude[bot] at 10 (live trial is its own gate 9); preamble math didn't match list. Also: Codex GitHub bot IS configured per live Tier-2 fire on PR #16, contradicting CLAUDE.md's stale "NOT configured" claim. | Preamble updated to "gates 2-9 + gate 10 = claude[bot] + Codex Tier-2 clean"; gate 10 wording updated to require BOTH bots clean; CLAUDE.md correction parked in BACKLOG PR #7 follow-ups (template touches need their own focused PR). |
| Tier-2 | 2 | **(a) fold** — Codex Tier-2 PR #16 #4 (P2): Verification gate 5 said "call-details-shaped fixture" but auto-accept on that fixture would hang/exit-2 on `manual_review_needed=true` files (`CLAUDE.md`, `pyproject.toml`); the iter-6 #5 fold split Bucket D into two fixtures (all-safe + call-details-shaped) but the Verification gate kept pointing at the wrong one | Gate 5 updated to "all-safe fixture (only `manual_review_needed=false` collisions; see Bucket D fixture (i))"; gate 6 already correctly points at the call-details-shaped fixture for stdin-injection. |
| Tier-2 | 3 | **(a) fold** — Codex Tier-2 PR #16 #5 (P1, on commit `d3e0bb5` after the iter-7-Tier-2 fold): Scope #5 rules (b)/(c)/(e) classified existing files as `WRITE`, but Bucket B's v2 restore matrix defines `WRITE` as a created-file policy that restores by DELETING the path. Running `--mode=adopt` then `--restore` against a target where rule (b)/(c)/(e) fired would delete pre-existing user files — direct violation of the restore safety contract. Three of five Tier-2 findings now traceable to this pattern: spec sections evolve independently; per-policy semantics in Bucket B don't get cross-walked against rule outputs in Scope #5. | Rules reclassified per their actual semantics: (a) WRITE (create — restore deletes); (b) OVERWRITE (replace empty content — captures `content_before` = b"", restore writes empty back); (c) SKIP (byte-identical match — no write, no manifest entry); (e) SKIP (byte-identical `.python-version` — same logic as c). Bucket D smoke-fixture row updated to match (`.python-version` → SKIP, empty-file → OVERWRITE, identical-content → SKIP) + explicit assertion that `--restore` does NOT delete pre-existing files. |
| Tier-2 | 2 | **(a) fold** — Codex Tier-2 PR #16 #6 (P2, on commit `ecab010`): my round-2 fold made the all-safe fixture description self-contradictory — said `.gitignore` is "all-skill-patterns-present → SKIP" AND that the fixture verifies APPEND_MERGE no-op, but rule (d) makes those mutually exclusive (patterns all present → SKIP; patterns missing → APPEND_MERGE). Self-introduced drift in the fold itself. | Fixture's `.gitignore` now has **some skill patterns MISSING** → APPEND_MERGE actually fires (auto-accept-safe since rule (d) is `manual_review_needed=false`). Verifies APPEND_MERGE real-fire, not no-op. SKIP-byte-identical coverage moved to a separate fixture file (e.g. matching `pyproject.toml`-shaped template). **Round 3 is the first Tier-2 round with zero P1.** |
| Tier-2 | 3 | **(a) fold** — Codex Tier-2 PR #16 #7 (P1, on commit `2045438`): Scope #5 rule (a) said "target file doesn't exist → WRITE" without checking `ignored_by_git`. iter-7 #1 added the `TargetMeta.ignored_by_git` field + described the precedence in Bucket B's recommend_policy contract, but the Scope #5 rule list itself never reflected the precedence. Result: a missing gitignored file like `AGENTS.md` would fall into rule (a) → `WRITE` → silently written to disk + invisible in `git diff --stat` because `.gitignore` excludes it. Same "spec sections evolve independently" pattern as Tier-2 #5. | New rule **(a0)** prepended to Scope #5: planned CREATE that is `ignored_by_git` → SKIP (default) OR WRITE_NEW with `manual_review_needed=true`; fires BEFORE rule (a). Rule (a) updated to "target doesn't exist AND NOT `ignored_by_git`". Precedence is now explicit in the rule list. |
| Tier-2 | 2 | **(a) fold** — Codex Tier-2 PR #16 #8 (P2, on commit `2045438`): my round-1 P1 fold made Phase B require "Phase C amended in a follow-up PR" for `manual_review_needed=false` misfires, but Phase 2 gate 9 requires the live trial to complete IN THIS PR. Result: misfire → unblockable deadlock (PR can't satisfy its own gates without external work). Self-introduced contradiction. | Phase B "Fix location" wording updated: heuristic fix is folded into THIS implementation PR by looping back to Phase C (amend recommend_policy + tests + smoke fixtures, re-run gates 2-8, then re-run Phase B). Follow-up PR is reserved only for owner-deferred out-of-scope cases (e.g. needing Phase A data from a second target project). |
| Tier-2 | 3 | **(a) fold** — Codex Tier-2 PR #16 #9 (P1, on commit `5ecd606`): Risks row "Heuristics misfire on call-details" still said "remediation is a follow-up PR" while Phase B (after round-4 fold) said "in-PR loop-back." Direct contradiction across sections. Same drift pattern as #5 and #7 — fix one section, leave the cross-referenced section stale. | Risks row split by misfire kind: (1) `manual_review_needed=true` misfire → owner rejects via prompt; UX gap in Section (e); follow-up PR optional. (2) `manual_review_needed=false` misfire → in-PR loop-back to Phase C per Sequencing Phase B. Explicit cross-reference to the Phase B fix-location wording. |
| Tier-2 | 2 | **(a) fold** — Codex Tier-2 PR #16 #10 (P2, on commit `5ecd606`): Bucket E Phase D step (3) recorded `git diff --stat -- <planned-files-only>`, but adopt mode also mutates `.new` files (rules (a0) and (f) WRITE_NEW) that are NOT in the planned-files list. Result: trial report undercounts real mutations, weakening rollback audit evidence. | Diff-stat scope expanded to `<planned-files> <manifest-target-paths>` where `<manifest-target-paths>` extracts every `target_path` from the live manifest's WRITE_NEW + APPEND_MERGE entries. Manifest IS the source of truth for what was mutated; still scoped (NOT full repo diff). |
| Tier-2 | 3 | **(a) fold + STRUCTURAL AUDIT PASS** — Codex Tier-2 PR #16 #11 (P1, on commit `19b930b`): Bucket E Phase A row STILL said "remediation is a follow-up PR, NOT a rewrite" while Phase B + Risks row (after rounds 4-5 folds) said in-PR loop-back for safe-policy misfires. **Third consecutive round catching the same cross-section drift class** (after #9 and #5). Hydra pattern: fix one row, leave another stale. **Audit pass**: grepped every section mentioning misfire/follow-up-PR/UX-gap. Found 2 stale rows (Bucket E Phase A line 108 — flagged by Codex — AND Scope #11 Section (c) description line 56 — would have been NEXT round's P1). | Both rows updated in lockstep with the canonical Sequencing Phase B + Risks row rule (the misfire-split): `manual_review_needed=true` misfire → UX gap in Section (e), follow-up optional; `manual_review_needed=false` misfire → in-PR loop-back to Phase C. Explicit cross-reference between sections so future folds maintain the link. Structural fix to break the hydra. |
| Tier-2 | 2 | **(a) fold** — Codex Tier-2 PR #16 #12 (P2, on commit `19b930b`): `git diff --stat` doesn't surface untracked files; `.new` files from WRITE_NEW are untracked. Even with the round-5 fold adding `<manifest-target-paths>` to diff-stat scope, `.new` files would still be invisible because diff compares index/worktree against HEAD, not against the empty set. | Phase D step (3) now records BOTH: (i) `git diff --stat -- <planned-files> <manifest-target-paths>` for tracked changes + (ii) `git status --short -- <manifest-target-paths>` for untracked `.new` files. Together they fully describe the trial's mutation surface. |
| Tier-2 | 2 | **(a) fold** — Codex Tier-2 PR #16 #13 (P2, on commit `93d4595`): Sequencing Phase D preflight had bare `ORIGINAL_SHA` literal without `$` expansion (`git checkout -b ... ORIGINAL_SHA`); verbatim execution would fail trying to find a ref named `ORIGINAL_SHA`. Bucket E already had `"$ORIGINAL_SHA"`. Cross-section drift, not a class drift; just a wording slip. | Sequencing Phase D updated to `"$ORIGINAL_SHA"` matching Bucket E. **Round 7 was zero P1 — structural audit pass on round 6 broke the hydra**; remaining 2 P2s are narrow real issues, not cross-section drift class. |
| Tier-2 | 2 | **(a) fold** — Codex Tier-2 PR #16 #14 (P2, on commit `93d4595`): Rollback recipe step (c) hardcoded `rm -f <target>/{CLAUDE.md,pyproject.toml}.new`, but rules (a0) and (f) can produce WRITE_NEW for any path (ignored creates, other domain-rich markdown files). | Manifest-derived cleanup: extract every `target_path` ending in `.new` from the live manifest via `jq -r '.entries[] | select(.policy=="WRITE_NEW") | .target_path'` and `rm -f` each. Survives future rule changes without recipe updates here. |
| Tier-2 | 2 | **(a) fold + canonical-recipe pointer** — Codex Tier-2 PR #16 #15 (P2, on commit `f2831e5`): Sequencing Phase D had stale `git diff --stat HEAD~0` while Bucket E (canonical executable recipe per iter-6 #6) had the new dual-capture (diff-stat + status-short for untracked). Same root cause as #13: two parallel Phase D recipes (Sequencing narrative + Bucket E executable) drift independently. **Round 6's structural audit covered misfire-remediation wording but not Phase D commands.** | Conservative fix: Sequencing aligned with Bucket E's dual-capture command + **explicit "canonical executable recipe lives in Bucket E" pointer in Sequencing Phase D**, so future command changes happen in Bucket E first and mirror to Sequencing (single source of truth). Eliminates drift surface without a full structural rewrite. |

## Lessons surfaced

(filled in as reviewer-proposed lessons accumulate)

## What we are NOT doing in this PR

- **No CLI policy table.** Content-driven analyze + interactive decide only.
- **No automatic content merge for non-`.gitignore` files.** `WRITE_NEW` is the safe recommendation; user merges manually.
- **No retroactive adoption mode for already-bootstrapped projects.** PR #7 is forward-only.
- **No `--mode=adopt` for Node or Go.** Python-first.
- **No prompt-toolkit dependency.** Plain stdin.
- **No undo within interactive session.** User quits + re-runs.

## Critical files to read before each iter's review

For Codex / Claude:

- `docs/plans/2026-05-18-skill-pr6-uv-support.md` — closest precedent; documents the BACKLOG (c) framing of analyze-then-decide-with-owner that PR #7 implements
- `docs/plans/README.md` — workflow doc
- `bootstrap_lib/_flags.py` — current flag structure
- `bootstrap_lib/cli.py` lines 34-101 — current `_resolve_mode` + collision-abort logic (the existing PR #1 contract)
- `bootstrap_lib/manifest.py` — current manifest format (PR #7 extends to v2)
- `bootstrap_lib/detect.py` (post-PR-#6) — `DetectionResult` NamedTuple class-syntax precedent
- `bootstrap_lib/render.py` — `render_all` produces the planned_files dict that adopt.py consumes
- `~/Desktop/Code/Boxette/call-details/` (the trial target — DO NOT EDIT; read-only inspection)
- `BACKLOG.md` PR #6 follow-ups section (c), (d), (e) — what PR #7 is supposed to close
- The 4 known collision files in `call-details/` (`.gitignore`, `.python-version`, `CLAUDE.md`, `pyproject.toml`) to confirm the empirical baseline; the 15 clean-write files are listed in the Context "Empirical collision baseline" subsection
