[project]
name = "dev-project-setup"
version = "0.1.0"
description = "Bootstrap a working dev workflow into Python / Node-TS / Go projects"
requires-python = ">=3.12"

[tool.setuptools]
py-modules = ["bootstrap"]

[tool.setuptools.packages.find]
include = ["bootstrap_lib*"]
